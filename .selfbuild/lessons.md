
## 2026-08-24 (loop 59, run-11)
- opencode worker can hang with zero events: attempt 1 ran the full 30-min
  timeout (exitCode -1, events:0) and a trivial `opencode run --format json`
  probe returned empty output. Do not let all 3 attempts burn; probe cheaply
  first, then fall back to branch + gh pr create (sanctioned by the automation
  prompt). Fallback delivered loop 59 end to end.
- .devagent-worktrees/TASK reuse hazard: createWorktree returns an existing
  worktree as-is without checking it is current (src/git/worktree.ts). Found a
  TASK worktree pinned at Loop 42 that would have produced a PR against an
  ancient base. Check `git -C .devagent-worktrees/TASK log --oneline -1`
  against origin/main before dispatching devagent task in a reused workspace;
  remove stale worktree + devagent/TASK branch first.
- Concurrent Orca runs race on backlog picks: run-12 merged PR #23 (loop 58,
  same item this run had chosen and implemented tests against) mid-flight.
  Before implementing, check `gh pr list --state open` as well as git log on
  origin/main; re-triage if a pick is already in flight.
## 2026-08-24
- Loop 60: prompt-injection features must thread through three dispatch paths (deps single-worker, workers/fanout.ts, orchestrator/executor.ts) plus the scheduler deps interface; a change to only one path silently diverges behavior between `devagent task` fan-out and orchestrated runs.
- Loop 60: implementStage's cfg is a structural intersection (`StageConfig & Pick<RunConfig,...>`) — adding a config knob requires touching the inline type at the call site in cli.ts, not just DevAgentConfig.
## 2026-08-25 (loop 62, run-18)
- Workspace vanished mid-loop (run-18 dir deleted after ~25 min while actively
  being worked). The hourly orca-selfbuild-cleanup explicitly skipped it as
  "orca run still open (protected)", so something ELSE deletes live selfbuild
  workspaces — suspect reclaim-disk.sh or a manual sweep. Recovery worked:
  `git worktree prune && git worktree add <path> <branch>` from the main repo,
  then rewrite files from session context. Keep file contents recoverable:
  commit early on long loops instead of one end-of-loop commit.
- devagent task hardcodes task id 'TASK' (src/task.ts) so its worktree branch
  devagent/TASK can only be checked out in ONE workspace at a time; concurrent
  Orca runs collide ("already used by worktree at ..."). Until TASK ids are
  unique per run, fall back to direct branch + gh pr create when another run
  holds the branch.
## 2026-08-25 (loop 63, run-19)
- Nested `claude -p` probe in Orca selfbuild workspaces fails with "API Error:
  402 [opencode-zen/claude-haiku-4-5] requires an opencode API key" — the
  harness routes claude CLI calls through opencode-zen and nested spawns have
  no provider key. Probe with `claude -p "ok"` (90s cap) before attempting
  devagent task dispatch; on 402 go straight to branch + gh pr create.
## 2026-08-25 (loop 64, run-21)
- Stacked-PR bases must use the real remote branch name: guessing
  `devagent/loop-62-worker-sandbox` failed GraphQL validation; fetch it with
  `gh pr view <n> --json headRefName --jq .headRefName` before `gh pr create`.
  Stacking on an unmerged PR's head works fine and keeps the diff minimal.
## 2026-08-25 (loop 66, run-23-retry)
- Phase 4 backlog was fully consumed by loops 58-65: every remaining item
  verified shipped in code (remote #31, sandbox #30/#32/#33, rebase-stack #29,
  lessons #27, flaky-guard #25, clusters #23) or resolved operationally
  (state bootstrap). When the backlog is exhausted, mine recorded lessons for
  the residual gap inside a shipped item instead of re-implementing.
- Nested claude -p probe in this workspace fails differently than run-19:
  not a 402 but "[claude-code:unrecognized_model]" with exit 0 and no output —
  treat ANY non-"ok" probe result as dispatch-unavailable and go straight to
  branch + gh pr create. Also: fresh workspaces need `npm install` before
  npm test (node_modules absent), and TS5076 (`??` mixed with `||`) is a
  runtime SyntaxError under tsx that fails every test file importing cli.ts.
## 2026-08-25 (loop 67, run-26)
- A passing worker probe does NOT guarantee task dispatch: `opencode run` replied "ok"
  in seconds, but both nested devagent task attempts stalled ~20 min each and exited
  with no output (worktree left clean). Budget rule: give task dispatch at most TWO
  worker attempts (~40 min wall clock), then fall back to direct branch + gh pr create.
- Run background dispatch with `nohup ... > logfile &` + polling; a foreground bash
  tool timeout silently orphans/kills the driver mid-flight and leaves a stale
  .devagent-worktrees/TASK to clean up.
# Lessons (ratchet-only; append, never edit)
## 2026-08-23
- Kitchen Loop (arXiv 2603.25697): spec-anchored loops beat metric-optimized ones; starvation gate (N empty iterations -> halt) and per-run circuit breaker are complementary.
- Implementer-written tests are insufficient: adversarial/fresh-context verification catches feature failure even with green unit tests.
- GitLab Duo Agent Platform now ships issue->MR autonomous flows + merge-conflict resolution; GitLab surface is competitive ground -> gitlab.ts publisher is a priority backlog item.
- macOS ships bash 3.2: quoted operands in arithmetic ($(( "0" + 1 ))) fail with 'operand expected'; avoid inner quotes around command substitutions in arithmetic context.
- test/gitlab.test.ts existed separately from integrations.test.ts — always `git ls-files test/` before assuming a module's coverage lives in one file.
## 2026-08-24 (loop 3)
- BridgeMCP pattern (bridgemind.ai): exposing pipeline state as ~10 small MCP tools lets any external agent observe/steer shared work; DevAgent now exposes board state via devagent_board (mcp.ts), more surfaces can follow.
- test/mcp.test.ts pins tools/list output exactly — new MCP tools require updating it (found as a red test, fixed in same loop).
## 2026-08-24 (loop 45)
- Gate skip paths returned passed:true with only a detail-string hint — evidence semantics must be structural (skipped flag), not textual.
- Rollback verification needs a round-trip proof (up->down->re-up); a single forward pass cannot catch non-idempotent migrations.
- Pinned assertions drift when a feature lands without its test update (devagent_answer/title) — run full suite before declaring a loop's implementation done.
## 2026-08-24 (loop 5)
- `devagent task --auto-pr` publishStage (src/cli.ts:412) pushes `devagent/task-<runId8>` but nothing creates that branch or commits worker changes left uncommitted on devagent/TASK -> push always fails with refspec error; PRs must be finished manually (commit on devagent/TASK + gh pr create). Candidate fix for a future loop.
- Task mode hardcodes ticketId TASK (src/task.ts:27), so a stale `.devagent-worktrees/TASK` from a crashed/prior run blocks every later run until removed by hand; archive uncommitted WIP before force-removing it (loop 5 archived run-5 WIP to .selfbuild/wip-run5-TASK-github-fetch).
- Competitor convergence (Devin Blueprints knowledge entries, Codex setup scripts, Copilot copilot-setup-steps.yml, Jules env inference): declarative per-repo test-command config is table stakes; G1 now supports devagent.json testCommand override + pyproject.toml detection (PR #15).
- Issue assignment is now the universal agent trigger verb (Copilot assign-to-agent, Devin intake); DevAgent missing its own host tracker (GitHub Issues) is the top Phase 4 gap.
## 2026-08-24 (loop 6)
- Stale `.devagent-worktrees/TASK` blocker recurred exactly as predicted: run-9 crashed mid-loop holding branch `devagent/TASK`, blocking run-10's worktree creation. Recovery recipe that worked: copy WIP to `.selfbuild/wip-run<N>-TASK-*`, `git worktree remove --force` + prune, re-apply WIP, full `npm test`, PR.
- A crashed run's uncommitted task output can be adopted wholesale by the rerunning iteration when the goal is identical — cheaper than re-running the worker, and the archive dir preserves provenance for the PR body.
- Orca self-build workspaces are ephemeral but `.selfbuild/` in the main checkout is the durable cross-run state: ledger, lessons, and WIP archives must all live there, never in the workspace-local `.selfbuild/`.
## 2026-08-25
- GitHub now renders live agent-session status ("queued/working/waiting for review/completed") under issue assignees and in Projects boards (Agent activity in Issues/Projects, 2026-03-26) — DevAgent board status vocabulary should mirror these states so external observers read it natively.
- Codex CLI 0.128.0 shipped `/goal` (2026-04-30), productizing the Ralph loop in-session; fresh-context bash loops still win for long runs — validates our worktree-per-iteration selfbuild design rather than arguing for change.
- ngrok bmo post-mortem: a named deferred-work bucket (OPPORTUNITIES.md) made deferral the path of least resistance; improvement claims need per-tool telemetry, not vibes — keep backlog items concrete and sized-to-one-loop so the research phase picks them instead of parking them.
## 2026-08-25 (loop 48 research)
- Devin 2.2 tests its own work via computer-use on a desktop before opening the PR, and Jules shipped CI Fixer (auto-detects failed Actions checks on its PRs, fixes in a loop) — self-verify-before-review is now table stakes; DevAgent's G-gates cover pre-push, nothing covers post-PR CI red.
- OpenHands v1.13.0 added a "ready-for-dev issue readiness gate" with type-specific criteria; issue-readiness scoring is a cheap, concrete backlog item that complements our GitHub Issues ingestion (loop 6).
- DGM keep-if-score-went-up loops risk Goodhart (grader special-casing reads identical to real skill gains) — our evidence gates must stay structural and hard to game, per the loop 45 skipped-flag lesson.
## 2026-08-25 (iteration 9 research)
- Copilot cloud agent decoupled branch work from PR creation (2026-04-01 changelog: "work on a branch without opening a PR" + Diff-before-review), and @copilot now fixes failing Actions checks in-place instead of stacking a new PR on top (2026-03-24) — DevAgent's --auto-pr publishStage should mirror both: commit/push the task branch first, then create the PR as a separate, independently retryable step.
- synth-loop (avasis-ai/synth-loop) validates our evidence-gated self-build design at small scale: GitHub Actions-scheduled evolve loop where every improvement must pass structural validation or the batch is auto-rolled back, and no feature is added speculatively — only in response to a measured failure pattern; also persists failed approaches across restarts ("BreadthTracker"), matching our lessons ratchet.
- Jules' Planning Critic (secondary agent critiques auto-approved plans before execution, 9.5% failure-rate reduction) and Factory's STRIDE-based automated security review on every non-draft PR are cheap-to-copy gate ideas; a plan-critic pass fits DevAgent's Phase 2 fan-out without new infra.
## 2026-08-27 (iteration 48 research)
- Post-PR CI remediation is now a hard baseline: Jules CI Fixer, Copilot /pr auto (1.0.79, 2026-08-10) self-paced CI loop, Copilot Mobile "Fix with Copilot" (2026-07-23), and Codex `@codex fix` all close the loop on failing Actions checks in-place; DevAgent's G-gates cover pre-push but nothing runs after publishStage, so any Actions red leaves a PR idle until a human pushes. CI Fixer is the next one-loop backlog item: a stage between publishStage and autoMerge that re-dispatches the worker on the same branch when `evaluateChecks` returns a failing check, capped by a wave budget.
- DGM evidence-gated archive design (jennyzzt/dgm, ICLR 2026) and darwinloop (ShyamRV, 2026-06) confirm a self-build loop's correctness comes from retaining stepping-stones that underperform today but unlock later descendants — validates our lessons ratchet plus the (already shipped) ledger; structural, non-bypassable gates per loop 45's skipped-flag lesson are what prevent the Goodhart failure mode the same paper warns about.
- Devin 2.2's `.agents/skills/` auto-loading + Jules' per-repo `AGENTS.md` parallel Copilot's `.github/agents/` custom agents and Codex's AGENTS.md auto-load — the convergent pattern is "declarative per-repo agent instructions discoverable from the repo root"; DevAgent's G1 already honors `devagent.json` but should also auto-load `.devagent/AGENTS.md` from the target repo on ingestion so a repo can ship its own plan/test conventions without a config file.
## 2026-08-27 (iteration 49 research)
- Codex CLI 0.147.0 `--approve-for-me` (independent-LLM reviewer auto-approves low-risk, denies critical-risk; ~200x fewer human interrupts) is the structural version of the Jules Planning Critic lesson; a G0 plan-critic pass before worker dispatch would compose cleanly with the existing Phase 2 fan-out without new infra.
- Codex harness-sandbox separation (credentials never enter the model execution env) and Devin 2.2 Security Profiles GA both confirm the directional value of loop 32+33 sandbox work and motivate a future `executor: 'secrets-stripped' | 'full'` split, but neither is a one-loop item — keep on the post-CI-Fixer queue.
- OpenHands Agent Canvas as a multi-repo control plane (OpenHands + software-agent-sdk + typescript-client + automation) shows the operational ceiling of "model-agnostic dispatcher over any agent backend"; DevAgent's adapter-layer design (PRD §9) is already on the same axis but the dashboard surface is bare — track for a post-v1 UX pass, not the next loop.
## 2026-08-27 (iteration 50 research)
- Devin "Manage Devins" (Aug 2026): a coordinator Devin decomposes a task and dispatches each piece to a full Devin in its own isolated VM; the parent reads each child's *full trajectory* to improve the next breakdown, and each child has its own session link — validates our fan-out direction (loops 58-65) and points at the missing piece: a `fanout/ingestChildTrails` step that pulls each worker's `worklog.jsonl` into the planner's next goal draft. One-loop-sized when scoped to ingestion only.
- OpenHands v1.13.0 (2026-08-13) shipped an Issue Readiness Gate with type-specific "ready-for-dev" criteria (bug: repro+expected+actual+env+logs; feature: AC+design+scope; chore: DoD+no open questions) — directly complements our GitHub Issues ingestion (loop 6) as a structural pre-dispatch gate that pairs with both G0 plan-critic (iter 49) and CI-Fixer (iter 48) without new infra. The right next loop over CI-Fixer because it stops unready tickets from burning credits in the first place.
- Codex CLI v0.147.0 (2026-08-06) project-trust gate closes CVE-2025-61260 (malicious repo redirecting `CODEX_HOME` to project-local `.codex/` and silently loading attacker MCP servers) by presenting a one-time trust prompt for unfamiliar dirs and skipping all project-scoped config layers on decline; combined with managed-auth credential isolation, this is the two-layer defense pattern DevAgent's G1 should mirror when auto-loading `.devagent/AGENTS.md` (per the iter 48 lesson) — load ONLY after an explicit trust decision, never on first ingest.
## 2026-08-27 (iteration 51 research)
- Factory shipped "Automated Security Review" on Droid (2026-06-11) — STRIDE-based, runs on every non-draft PR with inline severity/CWE/fix comments and a `/security-review deep audit` multi-agent mode that already produced real CVEs (CVE-2026-42876, workos/workos-node#1578). This is the missing piece the iter 9 lesson pointed at as a "cheap-to-copy gate": DevAgent's G2/G3/G4 cover backend correctness, but nothing structural covers security review before autoMerge fires — a `G5:STRIDE` stage (light STRIDE pass over the diff + optional deep-audit on `SECURITY=critical` label) is the natural next domain gate after the migration/flaky/queue work.
- Droid Shield 2.0 (2026-07-01) split a "risk" classifier (recall-optimized) from a "downgrade" classifier (utility-optimized) with LoRA on Qwen 3.6 35B A3B — confirms the "two-LLM-stage" pattern (judge + override) is now an industry-accepted shape for guardrail gates, exactly the Codex `--approve-for-me` (iter 49) and Jules Planning Critic (iter 9) pattern. Use as the structural template for any future DevAgent gate that needs to balance false-positive cost against recall.
- Cognition shipped Devin Security Swarm (2026-07-01) and Devin Security Vulnerability Remediation Program (2026-07-02) — multi-agent find→validate-at-runtime→ship-PR for vulns. Not a one-loop item, but it confirms the "agent that fixes its own findings" loop is becoming baseline, and a future DevAgent cross-loop that runs Shield-style secrets/SCA on every merged PR (rather than per-workflow) is now table-stakes for trust positioning.
## 2026-08-28 (orchestrator-loop driver fix)
- orchestrator-loop.sh originally referenced PREV_TAIL/LESSONS_CTX in the research prompt without ever setting them — research ran blind and re-picked PR #39's already-merged Q9 work, burning an orchestrate run on duplicate work. Fix: set both vars from ledger+lessons before phase 1, and add a phases 2-3 validator that rejects goals restating merged ledger entries.
- Killing a devagent orchestrate/task mid-run: kill the `npx exec tsx ... cli.ts` ancestor, not the leaf `claude -p` — the in-worker repair loop respawns leaf workers until the parent dies.
## 2026-08-28 (iteration 52 research)
- Three independent competitors converged on "planner reads each child worklog before drafting the next goal" in the same week: Factory session forking that keeps current session + Missions (Aug 14/20), Copilot `/tasks`+`/worktree` (Aug 10), Devin "Manage Devins" parent-reads-child-trajectories (iter 50). The iter 50 `fanout/ingestChildTrails` recommendation moves from "one-loop-when-scoped" to "industry-baseline plumbing" — ship as the next loop, pure ingestion (no new gate, no new infra) so every subsequent goal draft (including G5:STRIDE and G0 plan-critic) gets better automatically.
- GitHub Copilot reasoning-level-per-task (Aug 3) + Codex 0.150.0 `@` task references + Factory Droid Claude Opus 5 in Auto (Aug 7) all confirm the industry is now letting the dispatcher pick model and reasoning effort per subtask — validates DevAgent's existing per-worker model override path and motivates a future `reasoning_effort` knob paralleling it, but post-fanout-trail, not now.
## 2026-08-28 (iter 52 week 2)
- Factory `droid-action` v6 (2026-08-07) moved GitLab MR review posting INTO CI itself (no MR-write tool exposed to the model, `GITLAB_TOKEN` stripped from the review pass, all proposals/approvals/API results replayable from `.droid-debug/` artifacts) — this is the exact template DevAgent's CI-Fixer should follow when the review path gets built: the agent proposes, but a non-agent CI step publishes, and the publish step is the only thing that holds credentials. Track as the structural shape for the next gate after fanout/ingestChildTrails.
- Codex CLI 0.149.0 (2026-08-20) shipped an interactive `codex agents` dashboard (search/start/open/rename/stop with configurable shortcuts) — validates the "human-facing orchestrator surface" axis and confirms DevAgent's missing piece is a per-loop dashboard view (worklog+trail+gate-status), not new dispatch logic. Post-iter-52 trail ingestion, not now.
## 2026-08-28 (iteration 53 research)
- Devin Fusion (2026-06-29, updated 2026-08-07) "switch on context-compaction" is the cleanest cost-amortization pattern in the space: model switching is timed to cache-miss events so the model swap is "free". DevAgent analog — when the selfbuild orchestrator compacts its lessons context (a forced cache miss anyway), ingest the latest fanout child `worklog.jsonl` excerpts at the same point. The two cache resets become one, and the iter 50/52 `fanout/ingestChildTrails` recommendation costs almost nothing to ship. Implementation: a `compact-context` hook that copies the prior loop's trail digest into the next loop's planner prompt at the moment the lessons digest is regenerated.
- Devin Desktop 3.8.20 (2026-08-21) ships ACP terminal+edit sharing ON by default with the toggle not on the documented team-settings page — a real-world example of the cost of "share by default, opt-out later" for agent-to-agent handoffs. Any future DevAgent feature that lets workers hand off to external agents (e.g. cross-Loop CodeReview broadcast, or a shared orchestrator that calls out to a user's local Codex CLI) MUST ship deny-by-default and the toggle MUST live on the same managed-settings page operators actually read. Bake this into the review checklist for any future inter-agent path, not a one-loop fix.
- GitHub Copilot Agent Plugins 1.0 (2026-08-06) is an open standard (AWS + Anysphere + Microsoft + OpenAI + Vercel + Google) for packaging skills + MCP servers, portable across VS Code, Copilot CLI, Copilot SDK, Copilot app with `managed-settings.json` policy. The long-term shape: a `.devagent-plugin/` package can ship our G1 conventions + the `.devagent/AGENTS.md` payload (iter 48) so any Copilot/Codex/OpenCode worker consumes the same instruction set without being DevAgent-aware. Post-trail and post-iter-51 G5:STRIDE, but record now so the plugin layout gets designed with the gate interface in mind rather than retrofitted.
- OpenAI's "Codex as a platform" (Aug 19-25, 2026) explicitly opens the harness + `app-server` protocol for embedding into engineering/ops/security products, and the long-horizon write-up credits three durable markdown artifacts (spec / plan / documentation) for keeping a 25h, 13M-token single-agent run coherent. DevAgent already has the analog (lessons digest + worklog.jsonl + G0 plan-critic) but it isn't being read back into the next loop's planner; this is the iter 50/52/53 `fanout/ingestChildTrails` recommendation climbing from "useful" to "industry-baseline plumbing" and the iter 53 priority.
- OpenHands v1.14.0/v1.15.0 (Aug 17-21) renamed `microagent` → `skill` to match the industry term that Copilot/Codex/Factory/DevAgent are all converging on. Bake the term into any new DevAgent surface (we already use "skill" in skills/*.md naming) so cross-worker packaging is consistent when the iter 51 G5 gate or the eventual `.devagent-plugin/` ships.
- Self-improving loop literature (Ouroboros, Prime Agent Continual Harness, SIA, OpenAI Tax AI) all converge on the same structural answer: separate the "thing being improved" from the "thing doing the improving", gate every change through a review step, and only let the loop touch a bounded surface with a frozen outer anchor. This is the shape to keep copying for the G5:STRIDE stage, the CI-Fixer review path, and any future cross-loop that runs Shield-style secrets/SCA on every merged PR — the loop touches the eval set / harness knobs, never the gating policy itself.
## 2026-08-28 (iteration 54 research)
- Devin Outposts (alpha 2026-07-21, deep-dive published 2026-08-24) splits the agent's "brain" (always Cognition-hosted) from the "devbox" (Cognition cloud, customer VPC, or third-party Modal sandbox) — same agent harness routes to different execution environments via a queue. DevAgent's analog is `devagent task --remote` (PR #31) but only at the per-dispatch layer; a future `--remote-compute` knob could route worker execution to herdr panes (already wired via the herdr integration) the same way Modal does for Devin. Not the next loop — the iter 50/52/53 `fanout/ingestChildTrails` plumbing still ships first because every gate reads the same planner digest.
- OpenAI Codex 0.150.0 (2026-08-26) added `@`-mention task references + 0.149.0 (2026-08-20) added the interactive `codex agents` dashboard with search/start/open/rename/stop; the v0.150.0+ `Interrupt` hook on the top-level turn is the missing piece for a selfbuild loop — a worker that's about to spend more than budget on a doomed retry should be killable from the orchestrator without killing the parent. Map onto DevAgent: a `taskInterrupt` MCP tool or webhook that the orchestrator can fire when a worker's `trail.jsonl` shows it's looping on the same failure 3+ times, freeing the wave slot for the next leg. Post-fanout, record now so the kill path is designed alongside the trail-ingest path, not retrofitted.
- Factory Agent Effectiveness (2026-08-13, private preview) connects session spend to cycle time, work intent, and shipped artifacts via local signals (Jira/Linear + GitHub/GitLab); DevAgent already has the analog primitives (devagent_ledger MCP tool + PR-URL extraction) but they are query-only, not surfaced as a per-loop dashboard. Combined with the iter 52 Codex `agents` dashboard lesson, the right next dashboard surface is "per-loop view: goal + worklog + trail-digest + gate-status + spend" — same shape as the iter 50/52/53 trail-ingest recommendation, just consumed differently by the operator. Track as the human-facing surface for after the planner starts reading trails.
- Three "self-improving loop" patterns landed in the same week: Factory "Continual Learning" (every review finding, incident, and session feeds back into the org context; loop 50/52/53 already follows this shape via the lessons ratchet), Devin Fusion "switch on context-compaction" (model switch timed to a forced cache miss so the swap is free; iter 53's `compact-context` hook is the analog), and OpenAI's long-horizon write-up (durable spec/plan/docs as the coherence anchor across a 13M-token run; DevAgent's lessons + worklog + G0 plan-critic is the same triplet). The convergence is now strong enough that "no trail ingestion between loops" is starting to look like a defect rather than a feature, and the next loop should ship it as pure plumbing (no new gate) so subsequent G0/G5 work gets better automatically.
## 2026-08-28 (loop 52-54 trio + spawnCli fix landed)
- Three consecutive loop failures had a single root cause: the main worktree had uncommitted edits (the spawnCli/runCli/syncCli refactor from earlier in this session), so the orchestrator's `git merge` step aborted on "Your local changes would be overwritten by merge." Loops 53/54 then compounded by re-running the same plan against a worktree that still had stale state. Fix: orchestrator-loop.sh now auto-stashes any pre-loop local changes and ensures the main worktree is on `main` before the merge target is touched.
- The "claude-code:unrecognized_model" warning for `cmd/minimax/minimax-m3-free` is COSMETIC — the proxy at localhost:20128 does serve it (the model self-identifies as MiniMax-M3). The CLI emits the warning from its local model registry, not from the proxy. Dash variant (`cmd/minimax-m3-free`) is hard-rejected; slash variant works. Verified by direct test: `claude -p --model cmd/minimax/minimax-m3-free "What model are you?"` → "I'm MiniMax-M3, developed by MiniMax."
- Workers write real code: loop 52's T1 commit (`5de543a task T1: Add trail storage helpers under .selfbuild/trails`) shipped a 309-line diff across 5 files with 488/488 tests passing. The failure was upstream (merge-back, not implementation).
## 2026-08-28 (iteration 55 research)
- Codex 0.150.1 (2026-08-27) now treats retained images as part of the compaction token budget — confirms the "compaction is the cache-reset point" thesis from the iter 53 Devin Fusion lesson, and means the planned `compact-context` hook (loop 55 candidate) is the right place to read the prior loop's worklog excerpts back in. The two resets (lessons regeneration + trail ingest) happen in the same moment, so they cost one cache miss instead of two.
- Claude Code v2.1.248 (2026-08-27) ships `--restricted` (strips `Bash`/`WebFetch` unless explicitly allowed) and `experimental.cacheTtl` for agent frontmatter; OpenHands v1.16.0 (2026-08-27) replaced the all-on skill catalog with an explicit allow-list. Together they form the two-layer "deny-by-default + managed-settings trust toggle on the same page operators read" pattern that the iter 50/53 lessons called for — bake the pattern into the review checklist for any future inter-agent path (e.g. cross-Loop CodeReview broadcast, shared orchestrator → user's local Codex CLI).
- Factory Droid "Connect" (2026-08-25) + Agent Effectiveness dashboard (2026-08-13) prove the spend→cycle-time→shipped-artifacts mapping is now table-stakes for an operator dashboard; combined with the Codex `agents` dashboard lesson (iter 52) and the iter 54 `taskInterrupt` kill-path lesson, the right post-trail-ingest UI surface is one per-loop view (goal + worklog + trail-digest + gate-status + spend), not a new dispatch path.
- OpenCode v1.18.20 (2026-08-21) surfaces failed subagent calls with a resumable `task_id` — the structural substrate the iter 54 `taskInterrupt` kill-path would need. Record now so the kill path is designed alongside the trail-ingest path (loop 55 candidate) rather than retrofitted later; both want the same per-worker trail state.
- ROOT CAUSE "no claude-code session showing as running" (2026-08-28, user-reported): the herdr pane env file (src/integrations/herdr.ts) exported every buildEnv entry as `export KEY=val`. When the driver is launched under npm, process.env carries `npm_config_node_pre_gyp:cache` (colon key). zsh's `export` of a non-identifier key aborts the whole `source` with "not valid in this context", so the pane never got PATH/HOME and `claude -p` died at startup — no session ever appeared alive. Fixed in commit 443dee8 (renderEnvFile filters to /^[A-Za-z_][A-Za-z0-9_]*$/ + 2 regression tests incl. a real /bin/zsh source). Note: a nohup-launched driver has no colon keys, so it runs fine — the bug only bit npm-launched drivers.
## 2026-08-30 (iteration 57 research)
- Factory Droid Computers (persistent machines, BYOM + always-on background agents that "identify patterns in their work") confirms persistent-environment + trail-feedback is now industry-default; our `ingestChildTrails` (src/prompt.ts:135) landed in time. Next competitive differentiator is the review pipeline itself — Ouroboros (86.74% Terminal-Bench 2.1, open-source self-developing agent) gates all self-changes through a deterministic candidate-branch review pipeline with an Adversarial Eye + Review Panel split; compare our single G5 gate against that two-stage shape before adding any new surface.
- Codex now has async hooks that invoke MCP tools (recent releases) — the concrete substrate for the iter 54 `taskInterrupt` kill-path; design the interrupt webhook against the same MCP-tool shape so a future Codex-embedded orchestrator gets it for free.
- `.github/workflows/release.yml` verified 2026-08-30: release job has no `needs: test` and no test job — a red main still ships a broken semver tag (PRD Q21). Ranked #1 backlog item for iter 57 as a live-defect fix in the loop's own merge-back path.
## 2026-08-30 (iteration 58 research)
- Codex 0.151.0 (2026-08-29) fixes remote sandbox enforcement and adds a configurable grace period for MCP tool discovery, plus extensions that can inspect/replace MCP tool results pre-model — the intercept point for the iter 54 `taskInterrupt` kill-path just got more concrete; design the interrupt webhook as an MCP-result-inspecting extension shape so a Codex-embedded orchestrator inherits it.
- Factory "model routing belongs in the harness" (2026-08-24, /news/model-routing-belongs-in-the-harness): 58% aggregate cost cut, but ONLY because routing is cache-aware — blind routing costs 2.12-2.37x frontier baseline at turns 61+. DevAgent's worker adapters must treat any future model routing as a cache-aware decision at the harness layer (per our switch-model.sh), never per-call; "staying put is itself a routing decision."
- Copilot weekly (2026-08-28): Copilot CLI restores sessions "interrupted mid-turn" on a native Rust runtime, and shared @GitHub agent sessions run in Slack/Teams — resume-after-kill is now table stakes for CLI agents, reinforcing that our planned trail-ingest + taskInterrupt work should make an interrupted worker resumable (state on disk), not just killable.
## 2026-08-30 (iteration 58 research, competitive sweep)
- Devin Desktop now hosts external ACP agents (docs updated 2026-08-23) — delegated ops, direct provider billing. The competitive frame shifts from "agent quality" to "harness trust": DevAgent's per-PR evidence gates remain the differentiator while vendors converge on model breadth.
- Ouroboros (arXiv 2608.08311) binds review evidence to a diff fingerprint and invalidates prior evidence on every new write; DevAgent should do the same in auto-merge — G5/check-gate results must be keyed to the exact main...branch SHA so a rebase silently invalidates stale approvals.
- Copilot shipped per-model token breakdown + agent-activity usage API (2026-08-07/11) and OpenHands v1.15 added accumulated LLM cost tracking + typed conversation errors (capability vs infra vs timeout vs budget, 2026-08-21): the Q24 ledger outcome model should adopt OpenHands' error taxonomy now, before taskInterrupt ships, so failure evidence and spend share one vocabulary.
## 2026-08-30 (iteration 58 research, phase 1 sweep)
- OpenHands v1.40.0 (2026-08-01) shipped typed conversation-error classification and v1.44.0 (2026-08-27) preserves crash-recovery results on interrupted actions + per-conversation locks — both confirm the two iter-58 design debts (Q24 error taxonomy before taskInterrupt; worker state on disk for resume-after-kill) are now industry-standard shapes; OpenHands docs are the reference implementation to copy, not just inspiration.
- Copilot Aug-2026 shipped Agent Plugins 1.0 across VS Code/CLI/app (Aug 12), enterprise MCP allowlists (Aug 6), and comments-triggered automations (Aug 3) — vendor harnesses are converging on deny-by-default tool policy + event-driven triggers; DevAgent's sandbox allowlist work (loop 65) and trigger surface are aligned, the differentiator remains per-PR evidence gates.
- Jules changelog shows no new entries since 2026-03-09 (Gemini 3.1 Pro) — stale >5 months; Jules drops out of the competitive set for near-term differentiation purposes.
## 2026-08-30 (iteration 58 research, phase 1 sweep 2)
- Cognition pivoted hard to security + federal: Devin Security Swarm / Vulnerability Remediation (cognition.com/blog 2026-07-01/02), FedRAMP High In-Process (2026-07-13), plus acquisitions (TierZero automations, Poke) and its own frontier-model line (SWE-1.7, FrontierCode 1.1). Their differentiation is model + compliance, not harness gates — DevAgent's per-PR evidence-gate lane stays uncontested; do not chase model work.
- OpenHands v1.9-v1.16.0 (2026-08-03..27) shipped per-run LLM cost in Activity Log exports (#16351), structured error outcomes in Canvas (#16231), context-usage meter + manual compaction (#16311), and LLM pre-flight validation (#16417) — Q24 ledger outcome model (OpenHands error taxonomy + spend column) now has a reference implementation for every field; cost-in-ledger precedes taskInterrupt.
- Codex 0.151.0 (2026-08-29, openai/codex releases) verified in release notes: extensions "inspect or replace MCP tool results before they reach the model" (#41202), optional-MCP discovery grace period (#41199), structured MCP errors in app-server (#41196), nested subagent token usage counting toward root goal budgets (#41183). Design `taskInterrupt`/trail-ingest as an MCP-result-inspecting extension and budget nested worker spend at the root — Codex-embedded orchestrator inherits both for free.
## 2026-08-30 (iteration 58 research, phase 1 sweep 3)
- OpenHands v1.9-v1.16.0 release pages verified on GitHub: per-run LLM cost in Activity Log exports (#16351), structured error outcomes in Canvas (#16231), context meter + manual compaction (#16311), LLM pre-flight validation (#16417) — the Q24 ledger outcome model has a per-field reference implementation; adopt taxonomy + spend column before taskInterrupt ships.
- Codex 0.151.0 (#41202, #41183) verified via openai/codex releases: MCP-result-inspecting extensions and nested subagent token budgeting at root goal — design taskInterrupt/trail-ingest against that shape so a Codex-embedded orchestrator inherits both.
- ShipWithAI self-improving-loop post (shipwithai.io/blog/self-improving-agent-loop, 2026-07-04): analysis agent proposes diffs only, merge stays human-gated (matches our PR-only push policy); recurring failure triage = policy line OR janitor script, and a fixer failing twice means the bug is upstream of the fixer — loops 57/58 dying on the same release.yml goal indicates the attempt itself must carry pre-verified exact diffs, not retry harder.
- Factory research post (2026-08-27) and Droid tool-integration push (2026-08-25) confirm vendor lane is integration/model work; DevAgent's per-PR evidence-gate lane remains uncontested.
## 2026-08-30 (iteration 58 research, phase 1 sweep 4)
- Codex 0.150.0 (2026-08-26, rust-v0.150.0) shipped `Interrupt` hooks (#40511) that fire commands/MCP handlers when a top-level turn is interrupted — a second hook point for taskInterrupt alongside the 0.151.0 MCP-result-inspecting extension; design interrupt delivery against Interrupt-hook semantics (once per interrupted turn).
- Kitchen Loop paper (arXiv 2603.25697): 285+ iterations / 1,094 merged PRs with zero oracle-detected regressions via regression-oracle + drift control + automatic pause gates; every iteration ends with "is the system at least as good?" — add a regression check ahead of DevAgent autoMerge before trusting fully-merged boards.
- Osmani loop-engineering patterns to copy: TOCTOU-safe deletion check before every merge, and attempt-counter labeling (needs-attention after N failures) — loops 57/58 dying twice on the same release.yml goal means the next attempt must ship a pre-verified exact diff (Q21), not retry exploratory.
## 2026-08-31 (iteration 58 research, phase 1 sweep 5)
- Correction to sweep 1: OpenHands has no v1.40/v1.44 releases — releases page tops out at v1.16.0 (2026-08-27); the cited features (typed error outcomes, crash-recovery results, per-conversation locks) exist only as v1.14 structured error outcomes in Canvas. Always verify version numbers against the releases page before citing.
- Copilot Aug 28 posted "Upcoming changes to GitHub Copilot policies and billing" — watch for cost-model impact on the Q24 spend column design; Factory research post (Aug 24) claims 58% cost cut from harness-level model routing, confirming the governor lane but staying out of scope (no model work).
- Recommendation for iter 58: ship CI-Fixer (re-dispatch worker on failing evaluateChecks between publishStage and autoMerge, PRD.md:721) with failure rows keyed to the Q24/OpenHands error taxonomy — it closes the post-publish failure class that killed loops 49/50 and 57/58 and pre-builds taskInterrupt's vocabulary.
## 2026-08-31 (iteration 58 research, phase 1 sweep 6)
- OpenHands releases re-verified (GitHub releases page): v1.16.0 (2026-08-27) adds LLM provider selection + skill-catalog allow-list; v1.14.0 (2026-08-17) structured error outcomes + LLM pre-flight validation; v1.11.0 per-run LLM cost in Activity Log; v1.13.0 context meter + manual compaction. No interrupt-handling work shipped — taskInterrupt remains white space; the Q24 ledger outcome model (taxonomy + spend column) still has per-field reference implementations to copy.
- Cognition Aug moves: Devin Fusion harness (2026-06-29, ~60% cost cut via sidekick-agent dynamic routing), SWE-1.7 model, Security Swarm, FedRAMP High In-Process — all model/compliance lane; still no harness verification gates from any vendor.
- Copilot Aug 28 posted "Upcoming changes to GitHub Copilot policies and billing" (github.blog/changelog); watch for cost-model impact on the Q24 spend column design. Factory's Aug-24 post reaffirms 58% harness-level routing savings — governor lane stays out of scope per PRD.
## 2026-08-31 (iteration 58 research, phase 1 sweep 7)
- Google Jules shipped a CI Fixer (jules.google/docs/changelog, 2026-02-19): auto-detects GitHub Actions failures, fixes, and resubmits PRs in a loop — direct competitor precedent for DevAgent's CI-Fixer backlog item; it is validated demand, and other vendors already have it, so shipping it closes parity, not novelty.
- Copilot usage report added per-model token breakdown (github.blog/changelog, 2026-08-11) plus impact-dashboard ROI section (2026-08-07) — copy the per-model token breakdown shape for the Q24 ledger spend column; Codex 0.152.0 alphas (2026-08-29/31) carry no documented changelog deltas beyond 0.151.0.
## 2026-08-30 (iteration 58 research, phase 1 external sweep)
- CORRECTION to the v1.40/v1.44 lesson above: OpenHands releases page shows latest as v1.16.0 (2026-08-27); no v1.40/v1.44 exist. Durable content stands: v1.11.0 (2026-08-07) shipped per-run LLM cost in Activity Log (#16351) and a typed agent action for launching local/Cloud child conversations (#16380) — child-spawn as a typed action is industry confirmation of our fanout shape; verify release numbers against the releases page before citing.
- Copilot `forceRemoteSettingsRefresh` (weekly Aug 24, 2026) fails closed on unconfirmed managed-settings refresh — third deny-by-default trust instance after Claude `--restricted` and OpenHands allow-list; Devin Security Swarm (07.01) is the first competitor with an evidence-gated find→validate-exploitability→remediation-PR pipeline (closest competitive analog to DevAgent gates); Copilot CLI `/worktree` + `/rewind` without Git confirm resume-after-change is table stakes.
- arXiv 2026-08-28 batch empirically validates harness-over-model: 2608.26218 (Same Model, Different Harness), 2608.26197 (deterministic execution constraints), 2608.26480 (ledger-based zero-shot self-orchestration), 2608.26696 (five runtime governance primitives), 2608.26195 (cost-utility trajectory alignment) — cite as evidence base for DevAgent's evidence-gate positioning instead of building instrumentation to re-prove it. Also: Q21/release.yml fix failed in runs 57 AND 58 — fix must be a test-enforced invariant (unit test asserting release job needs-chain), not a bare workflow edit.
## 2026-08-31 (iteration 58 research, phase 1 sweep 8)
- Competitor sweep confirms CI-Fixer as iteration 58 pick: Jules CI Fixer (2026-02-19, changelog page frozen at 2026-03-09) is direct parity precedent; Copilot shipped comment-triggered agent automations (Aug 3) and agent Plugins 1.0 GA (Aug 12), raising the automation bar; OpenHands (tops out v1.16.0, 2026-08-27) and Cognition (no August posts, blog frozen 07-28) shipped nothing in the verification/interrupt lane — taskInterrupt and evidence gates stay white space.
- Factory research post "What it Takes for Coding Agents to Complete Large Software Tasks" (2026-08-27): agents achieve near-parity when judged by self-written completion standards — self-grading inflation warning; DevAgent's external `evaluateChecks` oracle is the defensible position, and the CI-Fixer must re-dispatch against real CI results, never worker-graded ones. Factory's "Agent Effectiveness" analytics (Aug 13) validates our Phase 4 operator-observability item as market-expected, not optional.
- Copilot Aug 28 "Upcoming changes to policies and billing" confirmed on github.blog/changelog/label/copilot — check its final terms before finalizing the Q24 ledger spend column; its Aug 11 per-model token breakdown in usage reports remains the copy-source for the spend-column shape.
## 2026-08-31 (iteration 58 research, phase 1 sweep 9 — primary-source re-verification)
- Copilot changelog verified directly (github.blog/changelog/label/copilot): August is billing/policy prep ("Upcoming changes to policies and billing" Aug 28, per-model token breakdown Aug 11, ROI dashboard Aug 7) plus automations (comment-triggered Aug 3, Agent Plugins 1.0 GA Aug 12, global model policy GA Aug 26). No CI auto-fix entries — CI-Fixer remains unshipped by Copilot.
- Jules changelog frozen at 2026-03-09; CI Fixer (2026-02-19) auto-detects/fixes/resubmits PRs in a loop — confirmed the direct parity precedent. Cognition blog frozen at 07-28 (M&A only). OpenHands v1.15.0/v1.16.0 (Aug 21/27) are provider-selection/UI releases, no verification-lane work.
- CodeTree (arXiv:2508.20828) empirically ranks execution feedback above LLM-judge feedback for loop improvement — reinforces that the CI-Fixer must re-dispatch on real CI results (evaluateChecks), never worker-graded output; also matches Factory's self-grading-inflation warning from sweep 8.
## 2026-08-31 (iteration 58 research, primary-source re-verification, sweep 10)
- Jules CI Fixer (2026-02-19, jules.google/docs/changelog) verified: auto-detects GitHub Actions failures on its own PRs and "operates in a loop — fixing, committing, and resubmitting" — direct parity precedent for PRD.md:721 CI-Fixer; Copilot changelog (github.blog/changelog/label/copilot) has NO CI auto-fix entries through Aug 28, so shipping CI-Fixer closes parity against Jules, not novelty.
- Factory "What it Takes for Coding Agents to Complete Large Software Tasks" (2026-08-27, factory.ai/news): agents reach near-parity judged by SELF-WRITTEN completion standards — self-grading inflation warning; combined with CodeTree (arXiv:2508.20828, execution feedback > LLM-judge) the CI-Fixer must re-dispatch on real evaluateChecks output, never worker-graded success claims.
- Copilot per-model token breakdown in usage reports (2026-08-11) is the copy-source for the Q24 ledger spend column; check the final terms of the Aug 28 "Upcoming changes to policies and billing" post before locking the spend-column design.
## 2026-08-31 (iteration 58 research, phase 1 sweep 11)
- Devin release notes (docs.devin.ai/release-notes, Aug 7/26) shipped automations queueing + production automations API + GitLab triggers and security scan controls with batch remediation APIs — PR/ticket lifecycle management is the vendor frontier this week; strengthens zombie-PR hygiene (PRD.md:758) as next pick now that CI-Fixer re-dispatch shipped in PRs #96/#97.
- Self-build ecosystem confirms our safety norm: Ouroboros "pattern register" records recurring failures as durable rows (error class, count, root cause, structural fix) promoted under the same commit gate — copy that 4-field shape for the failure-cluster reporting item (PRD.md:584); yoyo-evolve (github.com/yologdev/yoyo-evolve) hard-reverts any batch failing tests ("cannot corrupt itself"), matching gate-fail=discard-worktree.
- Codex 0.149.0 (2026-08-20, github.com/openai/codex/releases) shipped interactive `codex agents` dashboard (search/start/stop tasks) — multi-agent operator-observability now shipping at competitors; reinforces Phase 4 operator-observability item and Devin structured-error-outcomes echoes our taskInterrupt post-mortem.
## 2026-09-01 (iteration 58 research, phase 1 sweep 12)
- Devin 2026-08-26 batch shipped "Batch Remediation via v3 API" at org/enterprise scope plus "Allow PRs to Be Opened by Session Participants" org setting and a CI Failure Shortcut to PR review tab — competitor is consolidating PR/ticket lifecycle management into one v3 API; closest external analog to our failure-cluster reporting item (PRD.md:584) and validates its org-scope design.
- Devin Fusion (cognition.com/blog/devin-fusion, late June 2026) ships a frontier+sidekick parallel harness for cost-aware multi-model dispatch — ~35-60% cost reduction on Fable-level tasks; our parallel fanout covers correctness but not per-task cost routing, so a future `config.model`-aware fanout leg selection is the open gap (not yet a backlog item, but worth tracking next time a cost concern surfaces).
- Codex 0.152.0 (2026-09-01) cloud-task requests **reject untrusted backend URLs and disable redirects** — third fail-closed trust instance after Claude `--restricted` and OpenHands allow-list; the cluster is now wide enough that trust-boundary deny-by-default is an industry baseline, not a DevAgent differentiator; OpenHands v1.16.0 (Aug 27) shipped explicit skills allow-list (replacing "all-on" catalog) as a fourth instance.
- Factory "Why model routing must be in the harness" (2026-08-24) reports 58% cost reduction without quality loss by routing at the harness layer, not the model layer — direct empirical evidence that DevAgent's harness-over-model positioning is the right framing; pair with sweep 8 arXiv 2608.26218 ("Same Model, Different Harness") when external research is needed to justify the architecture.
- Recommendation for iteration 58 next pick: **Executor failure surface (taskInterrupt + failure evidence)** over Failure-cluster reporting over Operator-observability — loops 57/58 both died on the same release-gating goal with only `attempts: 3`-style evidence and PR #83 still had to mine the ledger by hand to diagnose; closing this unblocks the next loop's diagnostic gap immediately, where the other two are strategic backlog.
## 2026-09-01 (iteration 59 research, phase 1 sweep 13)
- Devin 2026-08-19 "Auto-Fix Includes CI Checks" embeds failing CI check *names* in the auto-fix prompt alongside review findings — copy that pattern when wiring structured failure evidence into repair prompts so the next repair pass can target the actual failing check (not a generic "fix CI" prompt); pair with our existing `evaluateChecks` CI-Fixer re-dispatch from PRs #96/#97.
- OpenHands v1.17.0 unified skills module + dynamic shell-command skill execution but is still silent on interrupt handling — `taskInterrupt` lane remains a genuine white space; Devin CI Failure Shortcuts (Aug 26, worklog "CI failed" rows deep-link to PR review tab with checks expanded) is the closest UX precedent to copy, not OpenHands.
- OpenAI Codex 0.152.0 cloud-task requests reject untrusted backend URLs and disable redirects — 4th fail-closed trust instance (after Claude `--restricted`, OpenHands skills allow-list, Copilot `forceRemoteSettingsRefresh`); cluster is now wide enough that any new DevAgent trust knob should default-deny with an explicit `*Allowlist` env, matching the industry baseline instead of inventing a new policy shape.
- Google Jules changelog remains frozen at 2026-03-09 (CI Fixer Feb 19 still latest) — Jules is effectively dormant; do not cite Jules parity as live competitive pressure in positioning copy; rely on Devin/Copilot/Codex/OpenHands for current-month evidence.
## 2026-09-01 (iteration 59 research, phase 1 sweep 14)
- Devin 2026-08-28 "Code Scan Validation Severities" lets scan profiles configure which finding severities require validation, with a threshold meter in the profile editor (https://docs.devin.ai/release-notes/overview) — copy the config-driven severity/threshold shape when wiring G2/G3/G4 severity classification, and lift the threshold-meter UX for the gate-config UI; do not hard-code gate severities in source.
- Codex 0.152.0 (2026-09-01) ships two new defaults worth copying: (1) planning tool `update_plan` is now **disabled by default** and must be opted into via `tools.update_plan.enabled = true` (https://github.com/openai/codex/releases) — any planning-style tool DevAgent adds should ship disabled by default so silent re-enable never surprises the operator; (2) rate-limit banners expose actionable buttons (check usage / manage credits / reset limits / manage plans) — pair with our `OBSERVE-provider-health` work so throttle state surfaces one-click recovery actions instead of dead text.
- Operator-observability converged industry-wide this week: Devin "Waiting Activity Status" (Aug 26) + "Session Subscribers" (Aug 21), OpenHands v1.16.0 "Show a run's live phase in automations" (Aug 27), Copilot "code review Resolution Reasons" (Aug 27), Codex rate-limit banners with action buttons (0.152.0 Sep 1). Long-running operator UX is now baseline, not a DevAgent differentiator; our Phase 4 operator-observability backlog item stays correctly framed but stop citing it as strategic separation.
## 2026-09-01 (iteration 59 research, phase 1 sweep 15)
- GitHub Copilot VS Aug 28 update (github.blog/changelog/2026-08-28) shipped Low/Medium/High thinking-effort knobs, org/enterprise custom-agent publishing with picker-side auto-discovery, Git agent inline reviews, and a "Manage models" view exposing capability + context + cost columns — the cost-column surfacing matches PRD Q24 spend-column direction; copy the "Manage models" pattern for our `config.model` picker once we ship per-leg cost routing.
- Codex CLI is iterating fast: 0.152.0 shipped 1 Sep and 0.153.0-alpha.1 + 0.153.0-alpha.2 also dropped the same day — our `codex` worker adapter should re-verify CLI flags on every minor bump; treat pre-release alpha drops as a soft signal that the next stable will change behavior under us.
- Cognition blog has been frozen for engineering posts since 2026-07-28 (last post = LTM partnership, M&A only); the Aug 26 webapp facelift is UX polish, not capability — stop citing Cognition for "this week's frontier" until a fresh engineering post lands.
## 2026-09-01 (iteration 59 research, phase 1 sweep 16)
- OpenHands v1.14.0 (2026-08-17) shipped **structured error outcomes** in the canvas — independent confirmation (alongside Devin Aug 19 "Auto-Fix Includes CI Checks") that competitor harnesses are converging on explicit failure-shape in repair prompts; the Executor failure surface (taskInterrupt + failure evidence) backlog item now has two external precedents to copy verbatim.
- Lilian Weng "Harness Engineering for Self-Improvement" (2026-07-04, lilianweng.github.io/posts/2026-07-04-harness/) lifts DGM (Darwin Gödel Machine) as the canonical self-build pattern: pool parent agents → modify their harness code → keep high-performers → repeat. Jumped SWE-bench Verified 20%→50% with a fixed `Claude 3.5 Sonnet` (no model upgrade). For DevAgent this is the theoretical backing for wrapping the `lessons.md` feedback loop in a `propose → evaluate → accept with held-in/held-out splits` guard so future lessons edits can't silently regress; copy the AHE anti-reward-hacking rule of read-only verifier / runs / LLM config — DevAgent already satisfies this implicitly (worktree tests are isolated; runs dir is gitignored).
- Codex CLI iteration velocity is now measurable: 0.152.0 stable + five pre-release drops (0.152.0-alpha.4 through 0.153.0-alpha.2) all landed Aug 30 → Sep 1, a window of ~36 hours — add a CODE-CLI-VERSION-DRIFT health note to the `codex` worker adapter docstring so future loop runs flag the stale CLI assumption on first boot.
## 2026-09-01 (human deep-dive, sentrux discovery)
- sentrux.dev (sourced 2026-09-01, https://sentrux.dev/#install) is a pure-Rust MIT architectural sensor, not an agent: scalar quality signal = geometric mean of 5 graph root causes (modularity Newman 2004, acyclicity Tarjan SCC, depth Lakos, equality Gini, redundancy), exposed via `sentrux check .` exit-0/1 gate, MCP tools scan/health/session_start/session_end, and `.sentrux/rules.toml`. Its docs' agent workflow: target the lowest-scoring root cause per change (gradient descent, no artificial stopping point). Wired into the loop as backlog item GRADIENT (PRD §17) with policy question Q38 and full profile in PRD appendix 19.4; self-build protocol section "Structural gradient sensor" added to docs/SELF-BUILD-LOOP.md.
- Scout blind spot root cause: the scout funnel is repo-bound (PRD §4+§17 + ledger tail + lessons) and its research whitelist names agent products only; sensor/harness/tooling-adjacent categories never enter the funnel, so no agent could have discovered sentrux unprompted. Standing fix: research prompts (scout + selfbuild phase 1) should always include an adjacent-category scan (sensors, MCP servers, harness tooling), and new adjacent-category tools enter via PRD §4/19 first so the funnel can see them next cycle.
## 2026-09-02 (iteration 59 research, phase 1 sweep 17)
- GitHub Copilot is converging cloud agent + Chat on github.com + Mobile into a single default-on, Sandbox-backed unified experience no earlier than 2026-09-28, with github.com chat migrating to agent sessions and retention extending to life-of-account; code-review default effort moves Lite→Balanced the same date (https://github.blog/changelog/2026-08-28-upcoming-changes-to-github-copilot-policies-and-billing/) — PRD §4 impact: trigger plumbing becomes commoditized overnight while validation-depth white space widens; keep positioning copy pointed at trust-per-PR evidence, not autonomy.
- Copilot CLI 1.0.81–1.0.82 (Aug 27–29, https://github.com/github/copilot-cli/releases) shipped a resilience/telemetry trio worth copying: startup offers to restore sessions whose CLI died (precedent backing our cc-guard sessionguard), hooks receive OpenTelemetry traceparent so child spans correlate across the tree, and keystrokes are no longer dropped on unfocused panes so tmux/agent multiplexers can drive background panes (industry endorsement of the herdr background-pane model); a run/trace id threaded through worker env would make our JSONL rows correlatable the same way.
- Bounded-context and quota-fallback converged again this week: Codex 0.152.0 adds per-MCP-tool `output_token_limit` with consistent truncation across session resumes plus approval reviews that preserve instructions/authz across history compaction (#41421, #41660; https://www.codex-docs.com/en/docs/changelog), and Factory Droid v0.209.0 (Sep 1) suggests an available Droid Core model when credits run out (https://docs.factory.ai/changelog/release-notes) — our 4000-char ratchet digests already match the first pattern; the second says Q24 per-leg routing should include a quota-exhaustion fallback leg, not just cost ranking.
## 2026-09-02 (iteration 59 research, phase 1 sweep 18)
- Copilot CLI v1.0.83-0 (2026-08-31, https://github.com/github/copilot-cli/releases) detects herdr as a terminal multiplexer distinct from tmux — Kitty keyboard protocol, color-scheme following, terminal progress, /copy, and notifications now work in herdr panes; first major-vendor first-class treatment of the herdr background-pane model DevAgent runs on. v1.0.83-1 (2026-09-01) additionally resolves relative `--add-dir`/`--plugin-dir` against the session cwd under `--worktree`/`--resume` instead of the launch dir — the same worktree-relative path rule DevAgent remote dispatch and cleanup-snapshot paths should follow.
- Factory Droid v0.208.0 (2026-08-29, https://docs.factory.ai/changelog/release-notes) runs mission-milestone validation steps even after a session is interrupted, paused, or resumed — validation state must be invalidated on resume, never trusted across an interruption; when wiring cross-board retry memory, requeued/rebridged goals must not inherit pre-requeue gate passes. v0.209.0 (2026-09-01) also defers org-settings load so the CLI accepts input before settings arrive (lazy-config startup).
- Jules dormancy reconfirmed with a URL change: the changelog now lives at jules.google/docs/changelog (the old /changelog returns 404) and its newest entry is still 2026-03-09 (Gemini 3.1 Pro) — keep treating Jules as dormant and update any citation pointing at the dead URL.
## 2026-09-02 (iteration 59 research, phase 1 sweep 19)
- Self-improving-harness loops are now a category, not a paper: neosigmaai/auto-harness, prakashkagitha/evolving-agent-harnesses (Self-Harness, Zhang et al. 2026, held-in 64%->82% but honest null on held-out), Darwin-Agent/HarnessX, DeepExperience/Harness-R1 (RL-trained harness engineer writing sandboxed lifecycle hooks), raphaelcsogitec/harness-evolver, SuperagenticAI/metaharness (all active 2026 Q3). Converged mechanics to copy for the lessons.md eval guard (sweep 16 recommendation): 3-step gate = self-maintained regression suite (>=80%) then must-beat-best-so-far full test then suite promotion (auto-harness, Tau3 0.56->0.78); durable regression tests auto-generated per fixed bug; anti-gaming = test traces never written to disk + critic fires on suspicious score jumps; writes scoped via allowlist; any DevAgent self-eval must report held-out, never held-in only.
- Codex 0.150.0 Interrupt hooks (#40511, github.com/openai/codex/releases) run commands or MCP handlers when a top-level turn is interrupted — third external precedent for the Executor failure surface (taskInterrupt) backlog item, with a concrete shape to copy: on-interrupt handlers plus evidence capture. 0.152.0 also makes thread/shellCommand timeouts client-configurable incl. >1h deadlines (#41384) and 0.151.0 #41183 rolls subagent token usage into root goals. Iteration-59 pick: Executor failure surface (rank 1) over watchdog regression coverage over provider model-id validation.
- Copilot VS Code August releases (github.blog/changelog/2026-08-31-github-copilot-in-vs-code-august-2026-releases/): Agent Plugins 1.0 is a portable agent-customization standard across "compatible agent clients"; Agent Host connects multiple windows to one live session and resumes Copilot/Claude sessions started in other apps; /rubber-duck asks a complementary model for missed edge cases (adversarial second-opinion lane worth copying for fan-out judging); hover shows per-model input/cached/output tokens (Q24 picker pattern). GitHub also GA'd a JetBrains surface literally named "Copilot harness" (Aug 24) — "harness" is mainstream vendor vocabulary now; DevAgent positioning stays on validation depth, not the word.
## 2026-09-02 (iteration 59 research, phase 1 sweep 20)
- Copilot CLI moved to a native Rust runtime (terminal UI stays TypeScript; Aug 24 weekly release, https://github.blog/changelog/2026-08-28-github-copilot-weekly-releases-august-24/) and Sep-1 CLI builds add `forceLoginOrgs` plus fails-closed `forceRemoteSettingsRefresh`; "Auto mode now adapts model selection as your task evolves during a conversation" (https://github.com/github/copilot-cli/releases) — in-conversation dynamic model re-routing is a mainstream default now, so Q24 per-leg routing should treat mid-run model switching as table stakes, not a stretch goal.
- Harness-evolution literature hardened: AHE full paper (arxiv.org/html/2604.25850v2, Terminal-Bench-2 69.7%→77.0%, beats Codex-CLI's human harness 71.9%) quantifies that edit authors cannot foresee their own regressions — regression-prediction precision 11.8% ≈ 2x random — so acceptance gates must be must-beat-best-so-far with file-granularity revert, never self-predicted safety; Self-Harness v2 (arxiv.org/html/2606.09498v2) fixes the rule as "improve held-in AND not degrade held-out, else reject and log" with an auditable harness lineage; TRIAGE (arxiv.org/html/2608.10178v1) shows the one-sided floored-at-seed guarantee comes free from editing inspectable files; SkillOpt applies held-out-gated edits to a skill document — the exact shape for the lessons.md eval guard. Kitchen Loop (arxiv 2603.25697, 285+ iterations, 1,094 PRs, zero oracle-detected regressions) contributes two loop mechanics to copy now: a starvation gate (loop goes monitor-only and alerts when remaining work needs external deps — validated at their iterations 112–127) and PR attempt counters that retire red PRs to needs-attention instead of retrying forever — direct precedent for the zombie-PR hygiene backlog item.
- OpenHands v1.16.0 (2026-08-27, https://github.com/OpenHands/OpenHands/releases) shows a run's live phase in automations and replaces the all-on skill catalog with an explicit allow-list (phase-level observability + bounded skill surface — both patterns we already hold via ledger rows and the lessons ratchet). Codex 0.152.0 ships actionable rate-limit banners (check usage / manage credits / reset limits / manage plans, #41742) and disables the update_plan tool by default (#41744) — harness simplification as a default. Devin release notes Aug 28 add MCP marketplace servers + a Manage-Personal-Automations org permission (governance surface, not capability; engineering blog still frozen since 2026-07-28); Jules newest entry still 2026-03-09.
## 2026-09-02 (iteration 59 research, phase 1 sweep 21)
- Copilot-in-Slack public preview (2026-08-21, https://github.blog/changelog/2026-08-21-the-new-github-copilot-experience-in-slack/) makes @GitHub a shared team agent session and GitHub a launch partner for "Slack Code" channels; repo admins can require an extra approval before PRs attributed to the Copilot app identity merge — chat is now a first-class trigger surface, and "approval gate on agent-identity PRs" is a governance pattern to fold into our autoMerge policy notes. Devin's 2026-08-26 batch remediation via v3 API (https://docs.devin.ai/release-notes/2026) is fleet-level CI-Fixer validation — our per-PR TASK-fix-<pr> is the same category at smaller scope.
- Codex 0.149.0 (2026-08-20, https://github.com/openai/codex/releases/tag/rust-v0.149.0) ships `codex agents` (interactive task dashboard), `codex queue` (queue messages into running local/remote sessions), and `codex doctor` (endpoint/proxy/network diagnostics) — task-level observability and preflight-diagnostics precedents; Copilot CLI 1.0.79 added `worktreeBaseRef` (worktrees default to HEAD, not the remote default branch — https://github.com/github/copilot-cli/blob/main/changelog.md), the same rule our task worktrees already follow; OpenHands v1.14.0 #16417 ships LLM pre-flight validation that blocks saving misconfigured model profiles — the exact shape for the provider model-id validation backlog item (Q32).
- New self-harness paper HSI (arxiv.org/html/2608.08466, github.com/TailinZhou/hsi): task-family harnesses hot-swapped through a fixed task-injection seam, with evolver + meta-evolver kept non-self-referential by a frozen outer anchor — maps to COMPACT_CONTEXT_MARKER (seam) and loop scripts living outside the worker-edited surface (anchor); Kitchen Loop contributes a cross-PR interaction detector (files added by one PR and deleted by another, revert commits, 3+-merger churn files) for failure-cluster reporting; the Self-Harness repo quantifies selection inflation at ~0.1-0.3, so any future lessons.md eval guard must de-inflate with an independent higher-replication re-eval, never accept a single-draw gain.
## 2026-09-02 (iteration 59 research, phase 1 sweep 22)
- Copilot code review can now approve pull requests (2026-09-01, public preview, Pro/Pro+/Max/Business/Enterprise; off by default, configurable per enterprise/org/repo; approval assessments appear in every Copilot review's overview comment — https://github.blog/changelog/2026-09-01-copilot-code-review-can-now-approve-pull-requests/). Machine approval of PRs is now a vendor knob, landing days before the Sep 28 default-on unified agent experience; DevAgent autoMerge policy notes should record that GitHub natively offers AI review->approval, so our moat stays gate-produced evidence (migration dry-runs, STRIDE, CI rollup), never the approval act itself.
- Phantom Guardrails (arxiv.org/abs/2607.13083): self-improving-harness proposers fabricate failures that never happened (15/60 runs when a rule-shaped pattern meets an open-ended rule set plus a failure-presupposing instruction; 0/60 without), and inside add-only accept loops the phantom guardrail re-enters and persists even without the instruction — suppression-only acceptance cannot see it. lessons.md is exactly such an add-only accept loop and CI-Fixer dispatches on failed-check names: both must verify a cited failure against ground truth (re-fetched check output / ledger row) before acting or accepting; never write a lesson or fixer dispatch that presupposes a failure.
- Recuris (arxiv.org/abs/2608.24876, github.com/Gen-Verse/Recuris): couples working memory (task progress) with experiential/skill memory so execution yields structured evidence that localizes failures to a specific memory component, and a fixed Meta-Agent turns that evidence into localized, validation-gated skill-memory updates — 35/37 model-benchmark pairs improved, gains widen with horizon length (+32.2 on longest tasks), long-horizon failures down up to 80%. Concrete shape for the future lessons.md eval guard: evidence -> localized edit -> validation gate, with failure evidence localized to the component that failed rather than whole-file rewrites.
## 2026-09-02 (iteration 59 research, phase 1 sweep 23)
- Copilot CLI Sep-1 build (github.com/github/copilot-cli/releases): keystrokes sent to an unfocused terminal pane are no longer dropped, so "tmux and agent multiplexers can drive a background pane" — first-class background-pane driving like herdr's; startup offers to restore sessions whose CLI crashed or restarted; plugin-contributed agents/skills/MCP servers are no longer dropped in non-interactive `-p` runs (headless/interactive parity); subagent `hook.start`/`hook.end` events are recorded on the subagent session and re-emitted on the parent (per-role event lineage). Devin Aug 28 (docs.devin.ai/release-notes/2026): code-scan history + validation controls and granular org/account permissions — validation surfaced as a first-class artifact, mirroring our gate-evidence positioning. OpenHands main repo still v1.16.0, but software-agent-sdk is at v1.44.1 (github.com/OpenHands/software-agent-sdk/releases, Aug 28): crash-recovery result is kept on the interrupted action branch (#4488) and the global lifecycle lock became per-conversation locks (#4570) — crash recovery must survive the resume, not reset. Factory v0.209.0 adds `/archive` session archiving from chat (same shape as our board archiving). Jules newest entry still 2026-03-09 — dormant.
- New self-improving-loop precedents harden the eval-guard mechanics: theprint/nfh-self-improvement-loop (github.com/theprint/nfh-self-improvement-loop) documents a production failure where a sub-agent ignored every prompt instruction, batched 10 changes, and merged everything at 100% approval — fixed by exit-1 shell guards, not prompts: generator/evaluator session IDs must exist and differ, verdict must be APPROVE before any merge, >3 changed files per cycle auto-rolls back, and the evaluator never sees the generator's rationale (rationale biases approval). Ouroboros (arxiv.org/html/2608.08311, Terminal-Bench 2.1 86.97%, 161-day live "Hope" deployment) keeps the launcher/supervisor outside the mutable agent repo, blocks every core change behind a diff-review whose staged snapshot is fingerprinted before AND after review ("each write invalidates prior review evidence"), and gives rollback its own recovery path. RHI (arxiv.org/pdf/2607.15524) shows pairwise comparison against the immediate predecessor revision is a cheap, sufficient evaluation signal — low-reasoning agents with a few RHI revisions beat max-reasoning effort at up to 60% lower cost, via better inter-agent information flow, not longer traces.
## 2026-09-02 (iteration 59 research, phase 1 sweep 24)
- lSAAGl/loop-harness (github.com/lSAAGl/loop-harness) is the closest OSS peer to our selfbuild loop: scheduled loops + worktree isolation, but the primary agent NEVER pushes or posts — it stages commits/PR_BODY/outbox action files and a separate verifier session must print an exact `VERDICT: PASS` string before the orchestrator ships; per-loop state files dedup stable item IDs (CI run ids, PR numbers, package@version) so duplicate triggers/re-runs are harmless; ships a built-in triage-ci loop (PR with verified CI fix or diagnosis) — copy staged outputs + machine-checkable verdict + idempotent item-ID dedup. Ker102/Harneloop v0.0.2 alpha (github.com/Ker102/Harneloop) checks candidate evidence twice — at attach AND at promotion, so deleted runs/missing artifacts cannot support a release — and after any promote, parallel candidates on the old version must rebase and produce fresh evidence (same "each write invalidates prior review evidence" rule as Ouroboros). BerriAI/self-improving-agent (github.com/BerriAI/self-improving-agent) enforces approval in the tool schema (`userConfirmedInThisMessage: true` or the executor throws; apply also refuses non-exact-once snippet matches) — a schema-gate complement to nfh's exit-1 bash guards.
- Version-stream conflation hazard when scanning OpenHands: the pypi `openhands` package versioning (1.17.0+, fed by software-agent-sdk PR merges) is a different stream from software-agent-sdk release tags (v1.44.1, Aug 28) — pypi compare-page hits are NOT new sdk releases; main repo stays v1.16.0. Competitor quiet confirmed 2026-09-02: Codex 0.152.1 (Sep 1) is a single Guardian/Node-REPL-policy bugfix with 0.153.0-alpha.1-.4 rolling; Devin newest still Aug 28 (CLI v3000.6.7 fixes MCP registry loading behind corporate proxies/TLS inspection); Jules changelog still ends at Gemini 3.1 Pro (2026-03-09); Factory v0.209.0's only uncovered delta is GPT-5.6 Sol as default model.
## 2026-09-02 (iteration 59 research, phase 1 sweep 25)
- Copilot model-governance churn: Aug 31 made model access billing-org-only for multi-org seats (github.blog/changelog/2026-08-31-copilot-model-access-update-for-github-team-plans/) and Sep 1 deprecated six models across all Copilot surfaces (Gemini 3.1 Pro, three Claude Opus/Sonnet, Raptor Mini) — config.model ids rot on ~monthly vendor runways and per-org availability, so Q32 dispatch-time validation is evidence-backed, not paranoia. Copilot code review also gained bot-authored-PR full agentic reviews, lost its 300-file/20k-LOC cap, and added resolution reasons (github.blog/changelog/2026-08-27-copilot-code-review-resolution-reasons-and-expanded-capabilities/); VS August adds org-level custom agents, thinking-effort controls, and Git-agent pre-PR review of uncommitted changes.
- Lessons-eval-guard prerequisites landed from three new OSS loops: mboss37/selfsmith gates on a metric contract (metric must print a plain number, print it identically twice, and score a known-good case above a known-bad one — else no downstream gate logic can save you), uses a statistics script as verdict (never LLM opinion), gate rejects by default, and a guardrail hook the loop physically cannot edit; DaizeDong/self-evolve fixes "LLM proposes, code adjudicates" with six named self-deception paths closed and the key limit that a green baseline has zero auto-accept headroom (quality-tier heterogeneous judges from different model families are the only remaining signal); azena-ai/self-improving-loop keeps a versioned genome (v001->v002), one lever per tick, and failing-gate-returns-to-lever-selection-never-forward.
- Landscape corrections: Jules ships a CI Fixer (auto-detects and fixes GitHub Actions failures on PRs it creates — jules.google/docs/changelog, entry older than the still-newest 2026-03-09 Gemini 3.1 Pro post; dormancy otherwise unchanged), so PRD §4's "tests only if configured" undercredits it; Devin Desktop changelog (docs.devin.ai/desktop/changelog, previously untracked surface) shows plan mode emitting a full reviewable Markdown plan with an explicit Implement button, and post_setup_worktree hooks copying .env/untracked setup into session worktrees before start — same hazard class as our bare task worktrees missing local setup.
## 2026-09-02 (iteration 59 research, phase 1 sweep 26)
- lonj7798/self-improvement (github.com/lonj7798/self-improvement) is a new tournament-selection loop precedent: sealed evaluation keeps `sealed_files` read-only via git-diff plus SHA-256 hash checks so agents cannot edit their own benchmark; a critic agent enforces harness rules before execution (one hypothesis per plan, never repeat the same approach family 3x, per-round diversity); a circuit breaker halts after consecutive no-winner rounds; tournament ties break to fewer lines changed, and losing branches are tagged before deletion. The approach-family counter is the concrete shape for the Q27 cross-board retry memory backlog item (carry failure class so the scout deprioritizes repeated approaches); SHA-sealed metric files are the missing enforcement layer for the lessons eval guard.
- 2026-09-02 quiet-day confirmed across all majors (Devin Aug 28; Codex stable 0.152.1 with contentless 0.153.0-alpha.1-.4; Factory v0.209.0; OpenHands sdk v1.44.1; Jules newest 2026-03-09, re-verified this sweep); the only uncovered Copilot Sep-1 delta worth keeping is per-build-tool sandbox grantlists — sandboxed Bazel/Bazelisk runs get their required cache and output paths granted inside the sandbox (github.com/github/copilot-cli/releases) — the pattern for our Phase-4 sandbox tightening when network/filesystem allowlists land.
## 2026-09-02 (iteration 59 research, phase 1 sweep 27)
- Correction to sweep 24 plus new Copilot deltas: Codex Sep-1 was NOT quiet — 0.152.0 (github.com/openai/codex/releases/tag/rust-v0.152.0) shipped per-MCP-tool `output_token_limit` (#41421), app-server `thread/shellCommand` timeouts incl. >1h deadlines (#41384), cloud task requests rejecting untrusted backend URLs and redirects to protect saved credentials (#41403), and automatic approval reviews that preserve user instructions/answers/valid authorizations across history compaction (#41660); 0.151.0 (Aug 29) lets extensions inspect/replace MCP tool results before they reach the model (#41202) and accounts subagent token usage toward root goals (#41183). Copilot Aug-24 weekly (github.blog/changelog/2026-08-28-github-copilot-weekly-releases-august-24/) moves the CLI onto a native Rust runtime (TS UI retained) with `defaultMode`/`defaultPermissionMode` session defaults and restore of sessions killed mid-turn; Sep-28 convergence (github.blog/changelog/2026-08-28-upcoming-changes-to-github-copilot-policies-and-billing/) retires chat data after 28 days in favor of life-of-account retention on agent sessions, folds three policies into one enabled-by-default policy, backs cloud agent with Sandbox, and flips code-review default effort Lite->Balanced. OpenHands sdk v1.44.x extras (github.com/OpenHands/software-agent-sdk/releases): `ask_oracle` tool (#3673), per-conversation `user_data_dir` preventing SingletonLock collisions (#4602 — same collision class as our TASK-id work), and validate_profile redacting API keys from error responses (#4506 — Q32 validation must not leak the key it validates). Cognition reported at ~$900M ARR with ~$800M planned burn, Devin writing ~95% of Cognition's own code with 7x code shipped in six months (cognition.com + press, Aug 28-29) — strongest public self-build datapoint; Poke acquisition (July 2026) orchestrates Devin sessions over messaging.
- AHE / Agentic Harness Engineering (arxiv.org/pdf/2604.25850): three observability pillars — editable harness components as files (revertible at file granularity), a layered drill-down evidence corpus instead of raw trajectory logs, and decision observability pairing every edit with a self-declared prediction verified against next-round outcomes (falsifiable contract). Terminal-Bench 2 69.7->77.0, transfers cross-benchmark and cross-model; ablation localizes gains to tools/middleware/long-term memory while system-prompt-only edits REGRESS — experience belongs in structure, not prose; direct warning for lessons.md-as-prompt digestion. GSME (arxiv.org/html/2607.13683) separates proposing from crediting: deterministic code owns all sampling/measurement/significance, three gates (validity re-runs infra failures, activation requires the patch to have actually fired, paired-2sigma credit on a sealed test scored once), and a quality-diversity archive keyed on (where x why) pathology rather than tasks fixed — anti-overfitting bias.
- Evo-Bench (arxiv.org/html/2608.09096), first harness-evolving benchmark, documents EARLY SATURATION: evolvers find high-quality structures in early rounds then introduce detrimental modifications later — evolve to a best checkpoint with file-granularity revert, never always-forward (TRIAGE, arxiv.org/html/2608.10178v1, floors every loop at the seed harness exactly this way). EvolveNet (arxiv.org/html/2608.04968) composes data-local harness adaptations through evidence-gated merges with a static auditor rejecting any candidate that touches model/endpoint/credentials. neosigmaai/auto-harness (github.com/neosigmaai/auto-harness) is a new overnight failure-mining OSS loop: run benchmark -> analyze -> improve -> three-step gate (eval suite + full-test val_score + suite promotion) -> record to results.tsv -> update learnings.md; suite promotion (the eval suite itself is promoted only when stable) is the new gate shape.
## 2026-09-02 (iteration 59 research, phase 1 sweep 28)
- mickeyyaya/evolve-loop (github.com/mickeyyaya/evolve-loop, 1,000+ autonomous cycles, Claude Code plugin): every failed cycle gets a failure-disposition contract — honest rejection / pipeline fault / operator-owned — and each class routes differently (retry, salvage, or escalate); plus a build handoff floor (a red build never reaches review), graduated remediation (repair a correct implementation's minor defect in-phase instead of discarding the work), quarantine for poison tasks that cannot pass, and typed routing authority keeping operator-owned control-plane work out of autonomous lanes (ADR-0074). Verdicts are deterministic check exit codes, never LLM opinion; phases communicate only through single artifacts. This is the concrete design for Q36 (PRD docs/PRD.md:826): classify TASK-fix-<pr> outcomes on the just-merged Q35 ledger rows and gate retry budgets on the class.
- 0xagentkitchen/kitchenloop (github.com/0xagentkitchen/kitchenloop) UAT gate shape for the future eval guard: a fresh agent with zero context, the weakest model, read-only access, and an isolated worktree verifies the feature from a user's perspective against a sealed test card; "unbeatable tests" are 4 layers Compile -> Execute -> Parse -> State Deltas (in their deployments 38 passing unit tests coexisted with complete feature failure, so state deltas are the layer that catches it); a bounded regression oracle answers "is the system at least as good as X?" and pauses the loop when no. lispmeister/loom final retrospective (github.com/lispmeister/loom): 72 generations, 1 promoted (1.4% success) — source-patch self-modification accumulates cruft and is path-dependent; successor CAMBRIAN regenerates the full codebase from a spec each generation with LLM diversity as mutation. Honest promote rates in self-mod loops are brutally low; report ours honestly in the ledger.
- OpenHands enterprise release-notes page (docs.openhands.dev/enterprise/release-notes) is a previously untracked surface that exposes sdk-era deltas absent from GitHub release notes: automation completion callbacks report accumulated LLM cost (#4311), conversation errors are classified by type for automation consumers (#4316 — same shape as our ledger outcome analytics), AST-backed shell command-name resolution ships as a security control (#3944). Quiet-day otherwise: Codex 0.152.0 remainder is package-style MCP server names (#41700) and credential-refresh progress incl. Bedrock reauth (#41239) with 0.153.0-alpha.1-.4 still contentless; Factory v0.209.0 / Desktop v0.166.0 (Sep 1) is bugfix-only with v0.208.1 a 60%-off GPT/Grok promo; Devin and Jules show nothing newer than already-recorded deltas.
## 2026-09-02 (iteration 59 research, phase 1 sweep 29)
- Copilot code review can now APPROVE PRs (github.blog/changelog/2026-09-01-copilot-code-review-can-now-approve-pull-requests/, public preview): every review carries a ready-to-approve assessment in the overview comment; when admins enable it, Copilot's approval COUNTS toward required-approvals branch protection, is DISMISSED when new commits land after it (same "each write invalidates prior review evidence" rule as Ouroboros/Harneloop), and is scoped per-file-path at repo level. PRD §4's Copilot row "human review mandatory" is now factually stale — the white space shifts from "who reviews" to "judgment vs evidence": our merge carries gate evidence, Copilot's approval carries opinion. Applied lesson for us: after TASK-fix-<pr> lands new commits, gates (G5:STRIDE, autoReview) must re-run on the post-fix diff, never reuse pre-fix verdicts.
- Cosmic-Game-studios/Itera (github.com/Cosmic-Game-studios/Itera-Universal-Self-Improvement-System-for-Coding-Agents) generalizes Karpathy autoresearch into an SWE loop with four copyable mechanics: tiered evaluation (cheap fast-loop evals every iteration, expensive full gates only before final keep — the cost shape for our board-level regression oracle); a fitness vector with a COMPLEXITY TAX (reject tiny metric wins that worsen the code — new keep/discard criterion); hypothesis ranking with exploit/explore/stabilize modes that switch on whether the loop is improving, flat, or recovering from failures; and log_iteration.py re-validating the ENTIRE ledger before keeping a new entry (append validates everything before it, not just itself). Our worklog/ledger/lessons/CLAUDE.md stack already matches its four memory layers (working/episodic/learned/procedural). Companion honesty caveat from prakashkagitha/evolving-agent-harnesses (Self-Harness, Zhang et al.): the evolved champion did NOT separate from 1-shot on held-out — selection inflation runs 0.1-0.3 and must be de-inflated with an independent higher-replication re-eval; the lessons eval guard needs held-out, not just held-in.
- 2026-09-02 quiet-day re-confirmed (sweep 29): Devin newest Aug 28; Factory v0.209.0/Desktop v0.166.0 (Sep 1) with one uncovered detail — CLI now starts on time when org settings load slowly, applying them once they arrive (fail-open on slow config, a deliberate latency-over-freshness tradeoff); OpenHands sdk v1.44.1 (Aug 28) still newest, minor extras worth one line — streaming deltas no longer fan out to every subscriber (#4689) and the plaintext-save warning now detects ALL secret-bearing fields, not just llm.api_key (#4618, extends the Q32 redaction family); Codex stable 0.152.1 with 0.153.0-alpha.1-.4 contentless; Jules changelog still ends 2026-03-09.
## 2026-09-02 (iteration 59 research, phase 1 sweep 30)
- tsurupong/halo (github.com/tsurupong/halo) is a new plugin-port autonomous-loop harness: task sources, executors, quality gates, sinks, and triggers are separate processes behind JSON-Schema contracts with per-plugin contract tests; gate-loop-audit treats agent self-modification of CLAUDE.md/PROMPT.md/.harness.yml/test files as a FAILURE (ADR-0004 — same enforcement family as selfsmith's uneditable guardrail: the loop physically cannot rewrite its own control files); L1-L3 autonomy profiles progressively unlock side effects (commit, then PR); and zero-billing CI runs three deterministic jobs (unit, loop-regression with a fixture executor returning fixed JSON in place of the LLM, contract) while the LLM-calling E2E stays a manual smoke test — the loop-regression-with-fixture-executor job class is the shape our regression-oracle backlog item should copy (test the loop machine, not the model).
- VictorVVedtion/ouro-loop (github.com/VictorVVedtion/ouro-loop) enforces boundaries via runtime hooks (exit 2), not prompt discipline, and ships cheap behavioral sensors we lack: root-cause-tracker warns at 3+ and 5+ edits to the same file (churn/oscillation), drift-detector warns on touching 5+ directories (scope drift), momentum-gate warns on 3:1+ read/write ratio (analysis paralysis), and recall-gate re-injects the constraint block right before context compaction (same pre-compaction rehydration idea as our lessons digest, but aimed at invariants, not history). These are Q34-family observability at near-zero cost — derivable from trail.jsonl tool-call sequences.
- Devin Fusion (July 2026, testingcatalog.net/cognitions-devin-fusion-gives-ai-agents-a-sidekick-and-cuts-costs-35/): a cheap sidekick model runs in parallel with the frontier lead (sidekick does exploration/tests/formatting, lead keeps planning/review, separate caches), and models swap mid-session during context compression for zero-cost upgrades — 35-41% cost cut; precedent for tiered model routing across our scout/builder/fixer roles rather than one config.model everywhere. Sweep-30 quiet-day confirm: Copilot newest remains the Sep-1 PR-approval post (VS August post already recorded), Jules changelog still ends 2026-03-09, Factory still v0.209.0, OpenHands sdk still v1.44.1, Codex stable 0.152.1 with contentless 0.153.0-alpha.1-.4; minor uncovered Codex 0.151.0 details: model-specific tool plans in ToolRouter (#41195) and configurable MCP startup grace (#41199).
## 2026-09-02 (iteration 59 research, phase 1 sweep 31)
- Copilot CLI Sep-1 build (github.com/github/copilot-cli/releases, beyond the recorded Bazel grantlists): keystrokes sent to an unfocused terminal pane are no longer dropped, explicitly "so tmux and agent multiplexers can drive a background pane" (herdr-family); hook.start/hook.end events from subagent hooks are recorded on the subagent's session AND re-emitted on its parent instead of vanishing; enterprise `forceLoginOrgs` pins sign-in to approved orgs; `forceRemoteSettingsRefresh` FAILS CLOSED — an undetermined policy posture blocks MCP servers, bypass mode, and plugin mutations rather than falling back to a stale cache; Auto mode adapts model selection as the task evolves (mid-conversation tiered routing, echoing Devin Fusion). Codex 0.152.0 addition we missed: #41309 honors required reviews when REUSING Guardian scores — review evidence reuse must re-verify the requirements it was certified against, extending the each-write-invalidates-review rule to the reuse path.
- Two new harness-evolution precedents: raphaelcsogitec/harness-evolver (Claude Code plugin, 5 adaptive proposers per iteration — exploit/explore/crossover plus two failure-targeted agents on the weakest task clusters) auto-generates regression tasks when a failure is fixed so "fixed bugs can never silently return", keeps per-task champions in a quality-diversity archive (not winner-take-all), and auto-triggers a Critic on suspicious score jumps to detect eval gaming. workofart/harness-experiment (henrypan.com/blog/2026-07-18-harness-training) trains the harness PyTorch-style: one bounded diff per epoch to a single policy file, StrictPareto criterion (accept only if NO baseline-solved task regresses AND the candidate solves more; ties break on cheaper solves), enforced patch surface (diffs outside the declared file list are rejected), all candidates kept in refs/candidates/, and determinism certification (seeded inference, frozen caches) so an outcome is attributable to the candidate alone.
- Devin docs.devin.ai/release-notes/overview is a previously untracked surface (only /desktop/changelog was): recent batch includes "Improve with Devin" — a Devin action that iterates on an EXISTING automation for you (agent improving its own automations, same self-build family), org-scoped batch remediation of code-scan findings via new v3 API routes, and an org setting gating whether session participants may open PRs. OpenHands v1.42.1/v1.43.0 (Aug 12/21, pre-dating recorded v1.44.x but uncovered): prompt-based evaluation hooks (#4160 — lessons-eval-guard precedent), goal loop no longer halts on a STUCK run (#4381), structured task outcome presets (#4479). Jules still ends 2026-03-09; Factory v0.209.0 adds only session archiving and a GPT-5.6 Sol default.
## 2026-09-02 (iteration 59 research, phase 1 sweep 32)
- lSAAGl/loop-harness (github.com/lSAAGl/loop-harness): the primary agent can NEVER publish — it commits in its worktree and stages outputs (PR_BODY.md, outbox action files like NN-pr-approve-.md); only after a second skeptical verifier session prints "VERDICT: PASS" does the orchestrator push/post. Plus per-loop scoped allowed_tools, idempotent state dedup on stable item IDs (safe duplicate triggers), and graceful degradation (a failing loop records and moves on, never kills the daemon). This staged-output/outbox trust boundary is the pattern our publishTaskPr/zombie-PR-hygiene work should copy: workers stage, the orchestrator ships after verification.
- Ker102/Harneloop v0.0.2 public alpha (github.com/Ker102/Harneloop): candidate evidence is checked when attached AND re-checked at promotion, so deleted runs or missing artifacts cannot support a release; parallel candidates based on an old promoted version must rebase and produce FRESH evidence before following it (each-write-invalidates-review extended to candidate interleavings); and "treat the produced thing as evidence" — inspect the produced artifact, never the agent's textual report (same family as kitchenloop state-deltas).
- OpenHands enterprise-notes deltas beyond sweep 28/31 (docs.openhands.dev/enterprise/release-notes): #4375 tracks requested_ref alongside resolved_ref (checkout-race observability — same class as our selfbuild-checkout-branch-races; record what you asked to check out vs what you got), #4362 configurable conversation worktree root, #4422 pre-flight LLM validation endpoint (Q32 family: validate a profile without leaking its key), #4492 read-at-use LLM provider connections, #4458 ConversationErrorEvent carried on run errors for automation callbacks. Codex 0.152.1 (Sep 1) has no changelog content; late 0.152.0 minors: planning tool now off by default via tools.update_plan.enabled (#41744), rate-limit banners with actions (#41742). Quiet day confirmed: Devin newest is the recorded Aug-25 batch (plus MCP-marketplace/Personal-Automations entries), Factory still v0.209.0, Jules top entry (Gemini 3.1 Pro default) predates the recorded 2026-03-09 cutoff, Copilot still the Sep-1 PR-approval + CLI build.
## 2026-09-02 (iteration 59 research, phase 1 sweep 33)
- Xiao et al. "Rethinking the Evaluation of Harness Evolution" (arxiv.org/abs/2607.12227, Aug 27): harness evolution does NOT consistently beat simple test-time-scaling baselines under matched feedback/inference budgets, and its gains vanish when search and evaluation tasks are disjoint — much of the reported lift is repeated sampling plus overfitting. Hardens the lessons-eval-guard backlog item: a candidate lesson must beat a matched-budget baseline (spend the same tokens on one unarmed extra attempt) and be judged on held-out tasks, or it is indistinguishable from more search. Companion precedent HarnessCompass (arxiv.org/html/2608.01918): a generalization gate makes task-specific edits inadmissible by construction (only task-agnostic changes can raise the score), component tracks are optimized separately and merged keeping only independently-beneficial loser edits (R3) — 54->66% Pass@1 in 5 iterations while transferring held-out.
- SHE "Trajectory-driven Safety Harness Evolution" (arxiv.org/html/2608.09885): decompose the harness into artifacts with EXPLICIT responsibilities (system prompt / rule bank / safety memory / tool policy), route each diagnosed failure to the responsible artifact for a bounded local edit, run a validity check that rejects reward-hacking shortcuts, accept a candidate only if it improves safety AND preserves task utility, and store rejected edits WITH reasons as feedback so later rounds stop re-proposing them (F_rej memory) — 3.1x ASR reduction, generalizes held-out. theprint/nfh-self-improvement-loop (github.com/theprint/nfh-self-improvement-loop) adds cheap cycle hygiene: mandatory pre-flight checks (timer set, fresh branch, state file present, verifier executable) before every self-modification cycle, and the builder agent is never its own judge.
- Quiet-day sweep 33 across all majors (Devin Aug 28; Copilot Sep-1 CLI + PR-approval; OpenHands sdk v1.44.1; Factory v0.209.0; Jules unchanged; Codex 0.152.1). Uncovered minors worth keeping: Copilot CLI 1.0.81 hooks receive OpenTelemetry traceparent/tracestate and emit correlated spans across parent and subagent sessions (Q12 provenance family; github.com/github/copilot-cli/blob/HEAD/changelog.md), and models.list now surfaces service-published per-model info/warning messages — provider-surfaced model-validity signal, direct precedent for Q32 dispatch-time validation; OpenHands enterprise telemetry rows #4376 (LLM/TOOL spans per ACP turn), #4459 (canonical conversation telemetry), #4529 (automation-conversation identification), #4483 (cache unchanged conversation summaries), #4486 (CI re-runs the description check when new commits land — each-write-invalidates in CI form).
## 2026-09-02 (iteration 59 research, phase 1 sweep 34)
- deghosal-2026/agent-self-edit (github.com/deghosal-2026/agent-self-edit, PyPI v0.1.0, created 2026-08-30) ships the concrete gate our lessons-eval-guard backlog item should copy: a sidecar proposes prompt edits from execution traces, but promotion runs through SIX deterministic fail-fast checks in code (not prompts) — sample floor, effect size, p-value, frozen sections, edit-distance limit, TF-IDF drift — with three outcomes (promote / near-miss-for-human-review / reject-with-reasoning-archived) and a SHA-256 versioned registry supporting diff/rollback/lineage. Its 15-iteration field test validated the gate by correctly rejecting every edit as statistically underpowered (p=0.23): an honest underpowered rejection is the guard working, so our v1 can start deterministic (suite green, 4000-char budget cap, churn cap) and add the statistical A/B later.
- nayyarsan/reflexloop (github.com/nayyarsan/reflexloop): cheap trigger discipline for self-modifying prompts — a weakness must cluster in >=3 of the last 20 sessions (rolling age-out window) before the refiner may edit; one minimal change per batch; prompt size is a hard CI check (<=80 lines) with a compression-only refactor mode that merges/removes rules but cannot add new ones; and a dedicated "Project-local rules" accumulation slot keeps the core constraints section stable — the size-cap-as-CI-check plus cluster-trigger are the two parts our lessons.md ratchet lacks (sweep-33 SHE's F_rej rejected-edit memory already covered).
- Competitor moves (sweep 34): Copilot is converging cloud agent + Chat on github.com + Mobile into ONE default-on unified experience no earlier than Sep 28, cloud agent moving onto Sandbox, chat retention extended 28 days -> life-of-account, and code-review default effort rises Lite -> Balanced (github.blog/changelog/2026-08-28-upcoming-changes-to-github-copilot-policies-and-billing/) — default-on consolidation raises PRD §4 trigger-surface pressure and makes "trust per PR" evidence the sharper differentiator. OpenHands main-repo release train v1.16.0 (2026-08-27, previously untracked surface alongside sdk 1.44.x/enterprise notes) adds automations live run phase (#16740), skills explicit allow-list replacing the all-on catalog (#16860), and a mock-LLM E2E in CI — fixture-executor loop regression family. Codex 0.152.1's only fix: Guardian approval review honors Node REPL policies from model metadata (extends the review-evidence-reuse family).
## 2026-09-02 (iteration 59 research, phase 1 sweep 35)
- theprint/nfh-self-improvement-loop full README (github.com/theprint/nfh-self-improvement-loop) goes beyond the sweep-33 pre-flight/never-own-judge notes: the generator's rationale is deliberately NOT shared with the evaluator (blind evaluation — knowing "the user really wanted this" biases approval), verify-agents.sh aborts if generator and evaluator session IDs match, hard-blocks.sh auto-rollbacks any cycle touching more than 3 files (batch detection), mode rotation (refactor/discover/combine) stops generator fixation, and approval-rate tracking catches approval creep; their production failure: a sub-agent given the architecture as TEXT ignored every rule, batched 10 changes, and merged with 100% approval — prompts are not guardrails, only exit-1 scripts are.
- lispmeister/loom (github.com/lispmeister/loom, final v0.2.0) isolates capability at BUILD level — the lab-worker build target excludes agent/self_modify.cljs, so Lab agents cannot even link spawn/promote/rollback (stronger than runtime hooks); Malli protocol schemas are immutable fixed points while loop/tools/prompts stay mutable; containers are the keep/revert boundary. Retrospective: 72 generations, 1 promoted (1.4%) — patch-level self-evolution is path-dependent cruft; successor CAMBRIAN (github.com/lispmeister/cambrian) evolves the SPEC and regenerates the codebase fresh each generation. BerriAI/self-improving-agent (github.com/BerriAI/self-improving-agent) gates apply_proposal behind four defense layers — schema gate requires userConfirmedInThisMessage:true and the executor throws on false, onBeforeApply hook, fine-grained PAT blast-radius cap — plus originalSnippet must match exactly once (stale-diff guard).
- Competitor sweep 35: Copilot Aug-24 batch (github.blog/changelog/2026-08-28-github-copilot-weekly-releases-august-24/) — CLI restores sessions that died mid-turn (crash recovery, sessionguard family) on a native Rust runtime; VS Code 1.135 continues recent Copilot OR CLAUDE agent sessions from other applications (cross-CLI session portability) and offers a "second opinion" complementary model (dual-model verification); VS August ships org-level published custom agents and per-model Low/Med/High thinking effort. Codex 0.152.0 #41660/#41846/#41852 preserve approval-review instructions/answers/authorizations across history compaction (review evidence must survive the compaction boundary, extending the review-reuse family) and #41403 rejects untrusted backend URLs for cloud tasks; 0.153.0-alpha.4 still contentless. Devin Aug-26/28 batches add model-degradation notifications (provider-surfaced validity, Q32 family), automations queueing + org-scoped CRUD/templates, a "lite" devin_mode API tier, and retire the v2 clone endpoint (410 Gone). Factory v0.208.1 discounts GPT and Grok models 60% (price pressure on the cost-opacity positioning). OpenHands still tops at v1.16.0 (v1.14.0 had already shipped LLM pre-flight validation #16417 in product form); Jules changelog still ends 2026-03-09.
## 2026-09-02 (iteration 59 research, phase 1 sweep 36)
- Five new loop projects converge on the same gate: prakashkagitha/evolving-agent-harnesses (Self-Harness, Zhang et al. 2026: fixed Haiku rewrites its own harness 64->82% on a SWE-bench Verified subset via mine-failures -> bounded edit -> verified-acceptance; round-3 edits ALL REJECTED -> harness carried forward unchanged — a no-op round is the gate working, and the model grew the draft->write_test->fix structure itself); mboss37/selfsmith (evaluate -> change -> gate -> repeat; one small reversible change per run; a dedicated skeptic agent backed by a real statistics check tries to disprove it, and outcomes are recorded win or lose); DaizeDong/self-evolve (constant methodology, adaptable signal source: one evaluate contract over three providers — program adjudication / anchor verification / generative heterogeneous judges — so no-ground-truth targets stay evaluable); neosigmaai/auto-harness (three-step gate: self-maintained eval suite + full-benchmark score + suite promotion, 0.56->0.78 Tau3; the eval suite itself is auto-maintained as the agent evolves); azena-ai/self-improving-loop (versioned genome.md v001->v002 = diffable strategy+state; levers.md always takes the top open lever; "state lives in a file, not a context window that evaporates").
- Codex 0.151.0 content the sweeps skipped (github.com/openai/codex/releases): #41183 counts nested subagent token usage toward ROOT goal budgets (cost attribution must roll up the session tree, not just the root's own turns), #41196 invalidates STALE Guardian classifications when permission state changes (an authorization snapshot dies with the policy it was computed against — extends each-write-invalidates-review to the authorization side), #41202 lets extensions inspect/replace MCP tool results before the model sees them. Copilot CLI 1.0.82 (github.com/github/copilot-cli/blob/HEAD/changelog.md) surfaces the specific auth failure (e.g. 401 Bad credentials) instead of only a /login prompt — diagnosis before re-auth, Q32 family.
- OpenHands enterprise 0.55.0 (docs.openhands.dev/enterprise/release-notes; same release prior sweeps quoted, detail uncovered): runtime reaping, DB pruning, and the idle-grace period are anchored on LAST ACTIVITY rather than creation time — our reaper/cleanup-stale should reap by last-activity, never by spawn age; plus daily conversation quotas with org-level exemptions and email-verified self-service increase requests (budget-cap UX, Duc persona). Quiet-day confirmed everywhere else: Devin Aug-28 + CLI v3000.6.7 (MCP registry behind corporate proxies/TLS inspection), Factory v0.209.0, OpenHands main v1.16.0, Jules still ends 2026-03-09, Codex 0.153 alphas contentless.
## 2026-09-02 (iteration 59 research, phase 1 sweep 37)
- SuperagenticAI/metaharness (github.com/SuperagenticAI/metaharness, OSS implementation of the Meta Harness paper): the outer loop optimizes the WHOLE executable harness (instruction files, setup/validation scripts, routing logic), not just prompts, and every candidate terminates in an explicit outcome — keep / discard / crash / timeout / no-change / scope-violation — where "no-change" (candidate identical to its parent) and "scope-violation" (edit outside the project's declared allowed_write_paths, auto-rejected) are the two terminal states our Q24 ledger taxonomy lacks; plus a compact environment snapshot before each proposal so agents don't burn early turns on workspace discovery, and a filesystem run store (prompts, candidate workspaces, validation + eval results, diffs, manifests) that keeps every optimization transition reviewable and reusable.
- Competitor sweep 37: Copilot CLI 1.0.83 (Sep 1; github.com/github/copilot-cli/releases) makes relative --add-dir/--plugin-dir paths resolve against the session working directory under --resume=/--worktree and after -C (path-resolution-under-resume class, same family as our remote-dispatch/worktree path bugs), adds the forceLoginOrgs managed setting (enterprise pins sign-in to approved orgs), and per-user budgets gained expiration dates (Sep-1 changelog). Factory v0.209.0 detail beyond "still v0.209.0" (docs.factory.ai/changelog/release-notes): sessions with no model selected silently default to GPT-5.6 Sol, /archive session archiving, and the CLI now starts on time when org settings load slowly, applying them once they arrive (async config apply — startup resilience family). OpenHands software-agent-sdk v1.44.1 concurrency internals (github.com/OpenHands/software-agent-sdk/releases): global _lifecycle_lock replaced by per-conversation locks (#4570) and unique user_data_dir per conversation prevents SingletonLock collisions (#4602) — concurrent-worker isolation on a shared host, same family as our TASK-id collision fix (PR #38). Quiet day elsewhere: Devin still Aug-28, Codex 0.153.0-alpha.4 contentless, Jules changelog unchanged, OpenHands main still v1.16.0.
- FairladyZ625/harness-anything (github.com/FairladyZ625/harness-anything) and kevinrgu/autoagent (github.com/kevinrgu/autoagent): "a failure should pay rent" — every captured failure must become a durable fact, decision, check, or preset, and completion is earned through gates declared by the task's resolved preset contract, with a six-field Submission Packet giving the reviewer a traceable claim plus inspection entry points (agents cannot close work by confidence alone); autoagent freezes an explicitly marked adapter section inside the single-file harness the meta-agent edits (frozen boundary at file-section granularity — human edits program.md, meta-agent edits agent.py, adapter never).
## 2026-09-02 (iteration 59 research, phase 1 sweep 38)
- Jules changelog resurrection after months of silence (jules.google/docs/changelog/): CI Fixer auto-detects failed checks on its own PRs, works a fix, and resubmits in a loop ("fixing, committing, and resubmitting") — the CI-Fixer family we shipped 2026-08-31 (PRs #96/#97) is now table-stakes parity at a major, so the differentiator is the bounded-attempts + ci-fix-failed ledger taxonomy, not the loop itself; also Gemini 3.1 Pro replaced 3 Pro as the Pro default (model churn keeps vindicating Q32 adapter-side validation), commit-authorship modes, and first MCP servers (Linear/Supabase/Context7/Neon/Tinybird/Stitch). Quiet elsewhere: Devin ends Aug-28, Codex 0.152.1 (Sep 1 22:33), Factory v0.209.0, OpenHands main v1.16.0. Copilot CLI Aug-31 ring adds automatic HTTPS-proxy mTLS client certs and detects the herdr multiplexer as its own thing so terminal features work in herdr panes (github.com/github/copilot-cli/releases); OpenHands sdk #4618 warns on ANY secret-bearing field saved plaintext, not just llm.api_key (extends NESTED_ENV_BLOCKLIST/env-scrub precedent to all config paths).
- Devin Aug-28 batch detail (docs.devin.ai/release-notes/overview): Code Scan profiles can configure WHICH finding severities REQUIRE validation, with a threshold meter in the profile editor, plus per-run scan history (profile + cost) and org/enterprise batch remediation v3 API — severity-gated validation has shipped at a competitor, i.e. real movement into our validation-depth white space (PRD 4.2); our G-gate severities must stay a first-class configurable surface (not hardcoded) or the moat erodes from the top.
- tathadn/self-evolving-codegen (github.com/tathadn/self-evolving-codegen): run the self-evolution loop ONLY on the agent whose output has a binary oracle (pytest passes in Docker or it doesn't) — Coder/Reviewer quality is subjective, so an LLM judge cannot be trusted to converge there; their gen-0 to gen-1 0.506 to 0.921 jump came from the evolver fixing output FORMATTING, not test strategy, and a tracker persists full history with rollback-on-regression. Scoping rule for lessons-eval-guard v1: judge candidate lessons only against deterministic oracles (suite green, gate results, ledger merge outcomes), and suspect formatting/plumbing before strategy when a digest change moves the needle.
## 2026-09-02 (iteration 59 research, phase 1 sweep 39)
- jstxw/Meta-Harness (github.com/jstxw/meta-harness): a durable execution runtime for self-improving loops whose whole value is VERIFIABLE liveness — workers claim branches with `FOR UPDATE SKIP LOCKED` + lease TTL, every claim increments a `lease_generation` fencing token, and every write carries it so a stale worker's late write is rejected by the data layer itself (no double execution even after kill -9 + lease reclaim); seven named invariants (no double execution, no orphans, resume convergence, fork isolation, lease safety, durable cancel, gap-free event sequence numbers enabling Last-Event-ID reconnects) are proven by a seeded fault-injecting simulator under a virtual clock — a failure prints its seed and the seed replays the exact interleaving — which found two real bugs pre-ship. Directly applicable to our duplicate-loop-race / park-requeue-deadlock / branch-race incident class: the loop runtime itself deserves deterministic simulation testing, and fencing tokens (not just TTLs) are what actually kill double-dispatch.
- china-qijizhifeng/agentic-harness-engineering (NexAU/AHE, github.com/china-qijizhifeng/agentic-harness-engineering): every meta-agent harness edit must commit FOUR fields — failure evidence (failing tasks + trace excerpts), root cause, targeted fix, and PREDICTED IMPACT (which tasks should flip to pass) — and staggered generations (runs/iteration_NNN mixes input/ from loop N-1 with evolve/ from loop N) attribute next-eval pass↔fail flips back to the responsible edits, auto-rolling-back predictions that did not hold; trace handling is digest-by-default (Agent Debugger distills ~10M-token traces into layered sourced reports) with drill-back to raw. This is the concrete mechanism lessons-eval-guard v1 was missing: lessons should carry a predicted-impact field and be falsified by subsequent ledger outcomes, not just capped at 4000 chars.
- Competitor moves (sweep 39): Jules shipped a Planning Critic — a secondary agent that critiques every plan NOT requiring human intervention BEFORE any code runs, reporting a 9.5% task-failure-rate reduction (jules.google/docs/changelog/) — plan-quality gating is now a shipped surface at a major, and DevAgent's headless path currently dispatches worker slots with zero plan critique; meanwhile Codex 0.152.0 DISABLED its built-in update_plan tool by default (#41744), confirming plan-churn is noise unless gated. Also: prakashkagitha/evolving-agent-harnesses honest caveat — the evolved harness champion did NOT separate from 1-shot on a separately curated held-out set, and selection inflation runs 0.1-0.3, so any future lessons-digest A/B needs independent re-evaluation before believing a win.
## 2026-09-02 (iteration 59 research, phase 1 sweep 40)
- JULES DATE CORRECTION (sweeps 38/39 were wrong about recency): jules.google/docs/changelog/ latest entry is Mar 09, 2026 (Gemini 3.1 Pro); CI Fixer is Feb 19, Planning Critic is Jan 26, 2026. Jules has been changelog-quiet ~6 months — the "plan-quality gating just shipped at a major" threat is 7 months old, not last-week's news. Rule: an entry appearing in a sweep for the first time is not the same as a new entry; read the on-page date before calling something a fresh competitor move.
- Copilot Sep.01 release (github.blog/changelog/): "Copilot code review can now approve pull requests" — review agents are being granted approval authority, which raises the bar for DevAgent's evidence-gated autoMerge story (our counter is validation evidence per merge, not reviewer confidence). Copilot CLI 1.0.84 same day persists Sessions-sidebar sorting across restarts; OpenHands software-agent-sdk v1.17.0 (github.com/OpenHands/software-agent-sdk) adds parallel-execution resource declaration for tom_consult (#2663) and Node-22 + deferred remote ACP init (#2656), with later SDK point releases testing split-brain conversation resume (#2943) and isolating tmux temp dirs per server (#2970) — the duplicate-worker-race family again.
- Long-running self-evolution datapoints: yologdev/yoyo-evolve (180 days, 200 -> 158k lines agent-written, tests-gated every 3h) sustains itself via trajectory awareness (extract_trajectory.py injects recent outcomes into planning), tiered memory compression (recent entries kept full, older compressed to themes, regenerated daily), and skill_evolve.sh that RETIRES skills not just creates them; GrayCodeAI/iterate (Go, 12h GitHub Actions cycle) does commit-if-green / revert-AND-JOURNAL-if-red, making the journal the durable failure-class memory that feeds the next plan; avasis-ai/synth-loop ranks harvested OSS design patterns as a gap list against its own codebase and implements only the top 3 per cycle, with a secret-scan hard-blocking every push.
## 2026-09-02 (iteration 59 research, phase 1 sweep 41)
- Copilot unification announcement (Aug 28) is the sweep-41 firsts vs. OUR lessons (no prior mention in any sweep): github.com Chat + Mobile + cloud agent converge into ONE default-on experience no earlier than Sep 28, cloud agent moves onto Sandbox, chat retention 28 days -> life-of-account, and code-review default effort rises Lite -> Balanced (github.blog/changelog/2026-08-28-upcoming-changes-to-github-copilot-policies-and-billing/) — default-on consolidation raises PRD §4 trigger-surface pressure; "trust per PR" evidence becomes the sharper differentiator.
- OpenHands v1.16.0 (Aug 27) adds Canvas Extensions frontend + Git Sync page + conversation overview panel, and Enterprise 0.55.0 confirms reaper anchors on LAST ACTIVITY (validates the sweep-36 lesson) — not creation time. Devin still Aug-28, Codex 0.152.1 (only #41403 credential-origin check), Factory v0.209.0 (archive, GPT-5.6 Sol default, async org settings). Jules still ends Mar 09.
- New self-loop patterns: Meta-Harness fencing tokens (lease_generation in data layer kills double-dispatch), AHE predicted-impact field (four fields per edit: failure evidence, root cause, targeted fix, predicted impact; staggered generations attribute outcomes), RHO self-preference from unlabeled trajectories (no ground truth), and harness-experiment PyTorch-style training (harness = trained module, git log = promotion history, criterion decides).
## 2026-09-02 (iteration 59 research synthesis)
- **Lessons eval guard is the single best next backlog item**: Current lessons digest is write-only; no verification that lessons help. Competitors (AHE, Meta-Harness) use propose→evaluate→accept gates with predicted-impact fields falsified by outcomes. **Why:** Without this, DevAgent risks negative learning where lessons silently degrade future prompts. **How to apply:** Implement `predictedImpact` field, evaluation pipeline against regression suite, acceptance criteria (must beat best-so-far on held-out tasks), and ledger logging.
- **Competitor parity requires outcome-based evolution**: Jules' Planning Critic (9.5% failure reduction), Meta-Harness's outcome tracking, and AHE's four-field edit commits show outcome verification is table-stakes. **Why:** Headless automation without verification is just faster broken code. **How to apply:** Add predicted-impact field to lessons, run regression evaluation before acceptance, and only inject verified lessons into prompts.
- **Fencing tokens and lease-based claiming prevent double-dispatch**: Meta-Harness's `lease_generation` fencing token kills double execution even after kill-9 + lease reclaim; proven by fault-injecting simulator. **Why:** DevAgent's duplicate-loop-race and park-requeue-deadlock classes need deterministic prevention. **How to apply:** Implement lease-based task claiming with TTL and fencing tokens in data layer; reject stale writes carrying old generation tokens.
- **Structural validation must be first-class**: Competitors add structural quality gates (Jules Planning Critic, STRIDE at Factory) because model-only validation is insufficient. **Why:** Backend correctness requires both functional tests AND structural safety checks. **How to apply:** Elevate GRADIENT structural sensor from advisory to blocking gate once track record builds; integrate with STRIDE and migration gates.
- **Lessons-eval-guard is the single best next backlog item**: Current lessons digest is write-only; no verification that lessons help. Competitors (AHE, Meta-Harness) use propose→evaluate→accept gates with predicted-impact fields falsified by outcomes. **Why:** Without this, DevAgent risks negative learning where lessons silently degrade future prompts. **How to apply:** Implement `predictedImpact` field, evaluation pipeline against regression suite, acceptance criteria (must beat best-so-far on held-out tasks), and ledger logging.
## 2026-09-02
- **Lessons-eval-guard v2**: Implement `predictedImpact` field in lessons, evaluation pipeline against regression suite, acceptance criteria (must beat best-so-far on held-out tasks), and ledger logging. Competitors (AHE, Meta-Harness) use propose→evaluate→accept gates with predicted-impact fields falsified by outcomes. Outcome verification is table-stakes for headless automation.
- **Fencing tokens for task claiming**: Implement lease-based task claiming with TTL and fencing tokens in data layer; reject stale writes carrying old generation tokens. Meta-Harness's `lease_generation` fencing token kills double execution even after kill-9 + lease reclaim; proven by fault-injecting simulator.
- **Structural validation as blocking gate**: Elevate GRADIENT structural sensor from advisory to blocking gate once track record builds; integrate with STRIDE and migration gates. Competitors add structural quality gates (Jules Planning Critic, STRIDE at Factory) because model-only validation is insufficient.
---
name: iteration-59-competitive-research
description: Competitive landscape analysis and self-improving loop patterns for iteration 59 research phase
metadata:
  type: reference
  created: 2026-09-02
## 2026-09-02 (iteration 59, Sep 1 delta)
- Copilot code review can now APPROVE PRs (Sep 1 public preview): every review carries a ready-to-approve assessment, counts toward `required-approvals` when enabled at enterprise/org/repo+path when enabled, dismissed on new commits like human approvals (github.blog/changelog/2026-09-01-copilot-code-review-can-now-approve-pull-requests/) — PRD §4 "human review mandatory" is now stale; white space shifts to judgment vs evidence. After TASK-fix-<pr> lands commits, gates must re-run before relying on prior approvals.
- Codex 0.152.0 (Sep 1, github.com/openai/codex/releases/tag/rust-v0.152.0): per-MCP-tool `output_token_limit` (#41421), `thread/shellCommand` timeouts >1h (#41384), cloud tasks reject untrusted backend URLs/redirects to protect credentials (#41403), approval-review transcript preserved across compaction (#41660), planning tool now opt-in (#41744) — output-budgeting and credential-origin checks are the two WorkerAdapter patterns to mirror.
- Claude Fable 5.1 GA in Copilot (Mythos class, Sep 1; github.blog/changelog/2026-09-01-claude-fable-5-1-generally-available-in-github-copilot) plus Sep 1 deprecation wave (Gemini 3.1 Pro, Opus 4.5/4.6, Sonnet 4.5/4.6, Raptor Mini → successors; github.blog/changelog/2026-08-31-selected-github-copilot-models-deprecated/) — provider-unqualified aliases now 400/403 at the adapter boundary; validate `config.model` at dispatch per Q32 instead of mid-board.
## 2026-09-02 (iteration 59 research, phase 1 sweep 42)
- Copilot August changelog (github.blog/changelog/label/copilot/): code review resolution reasons + expanded capabilities (Aug 27), Copilot harness GA in JetBrains (Aug 24), Agent Plugins 1.0 across VS Code/CLI/app (Aug 12), Copilot memory in JetBrains (Aug 11), reasoning-level customization for the cloud agent (Aug 3), MCP allowlists in managed settings (Aug 6), and per-model token breakdown in the usage report (Aug 11) — the last one is metering transparency that weakens PRD §4 white-space claim #4 ("cost opacity punishes this workload"); DevAgent's cost story must stay evidence-per-PR, not just predictability.
- Codex 0.152.1 (Sep 1, github.com/openai/codex/releases/tag/rust-v0.152.1) is a single fix (Guardian approval review honors Node REPL policies from model metadata) with 0.153.0-alpha.1..5 pre-releases in flight; Cognition blog-quiet since Jul 28; Jules changelog now sign-in-gated at jules.google.com/docs/changelog/ (monitoring surface lost, last dated entry Mar 09, 2026).
- Live evidence for lessons-eval-guard from our own repo: .selfbuild/lessons.md now carries duplicated near-identical synthesis bullets (the lessons-eval-guard recommendation appears 3x; sweep-38/39 content repeated verbatim in the injected memory block) — a write-only ratchet spends the 4000-char digest budget on repeats, so the guard must dedupe on content similarity BEFORE append, not just gate on predicted impact.
