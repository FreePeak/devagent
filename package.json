{
  "name": "devagent",
  "version": "0.1.0",
  "description": "Autonomous backend delivery agent: ticket to tested PR",
  "type": "module",
  "bin": {
    "devagent": "./dist/src/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "selfbuild": "bash scripts/selfbuild-loop.sh",
    "reclaim": "bash scripts/reclaim-disk.sh clean",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
